"""
Module Title
============

Short idescription.

Reference:
1. https://www.python.org/dev/peps/pep-0008
"""

# Imports order:
# 1. Standard library imports.
# 2. Related third party imports.
# 3. Local application/library specific imports.
import logging

# INFO, WARNING, ERROR and CRITICAL will be streamed to console.
LOGGER = logging.getLogger(__name__)
LOGGER.setLevel(logging.INFO)

# Example function
def add(x, y):
    """
    Function short description.

    :Parameters:

        1. First addend
        2. Second addend

    :Returns: ``None``

        1. Sum

    """
    # example code: logger
    LOGGER.debug("debug") # debug logs
    # NOTE: info should not exceed the total number of the "="s below;
    # the number of "="s below is the max characters in a line in 
    # Windows command-prompt.
    LOGGER.info("==============================================================================")
    LOGGER.warning("warning") # general warnings
    LOGGER.error("error") # user errors
    LOGGER.critical("critical") # code errors

    # actual code
    LOGGER.debug(x)
    LOGGER.debug(y)
    sum_ = x + y
    LOGGER.debug(sum_)
    return sum_
