""" home.copy.imagehandling
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen
# from . import (
    #
# )

screen = Screen()

locator = screen.add_locator("Mirror Image", marker=True)
locator.widget = "text-id='TXT_MIRROR_IMAGE'"

locator = screen.add_locator("Mirror Image Switch")
locator.area = "ScrollView"
locator.widget = "resource-id='com.lxk.workflow:id/mirror_control' && class='android.widget.ToggleButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify checked='%s'"
)
