""" home.copy.pagesperside
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen
# from . import (
    #
# )

screen = Screen()

locator = screen.add_locator("Pages per Side Orientation", marker=True)
locator.widget = "text-id='TXT_PAPER_SAVER_ORIENTATION'"

locator = screen.add_locator("Paper Saver Switch")
locator.area = "ScrollView"
locator.widget = "resource-id='com.lxk.workflow:id/paper_saver_switch' && class='android.widget.ToggleButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Print Page Borders")
locator.area = "ScrollView"
locator.widget = "resource-id='com.lxk.workflow:id/paper_saver_page_border_check' && class='android.widget.CheckBox'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify checked='%s'"
)
