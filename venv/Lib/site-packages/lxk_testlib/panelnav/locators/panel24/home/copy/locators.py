""" home.copy
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

screen = Screen()

locator = screen.add_locator("Screen Title", marker=True)
locator.widget = "text='Copy' && textid='69979'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Menu List Item")
locator.widget = "text='%s' && type='ListItem'"
locator.add_action(
    action=Action.SELECT,
    specifier="v-select"
)


locator = screen.add_locator("Setting Value")
locator.area = "list:index=2"
locator.widget = "type='ListItem'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify settingValue='%s'"
)
locator.add_action(
    action=Action.PRESS,
    specifier="h-select:data_type=int:set settingValue='%s'"
)

locator = screen.add_locator("Option Value")
locator.area = "list:index=2"
locator.widget = "text='%s' && type='ListItem'"
locator.add_action(
    action=Action.PRESS,
    specifier="h-select:option_count=%s"
)

locator = screen.add_locator("Copy To Value")
locator.widget = "text='%s' && type='ListItem'"
locator.add_action(
    action=Action.SELECT,
    specifier="h-select"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Slider Value")
locator.area = "list:index=2"
locator.widget = "selected='true' && type='ListItem'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify settingValue='%s'"
)
locator.add_action(
    action=Action.SELECT,
    specifier="h-select:data_type=int:set settingValue='%s'"
)

locator = screen.add_locator("Edit Box")
locator.widget = "type='EditBox' && selected='true'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify value='%s'"
)

locator = screen.add_locator("Enter")
locator.text = "%s"
locator.add_action(
    action=Action.PRESS,
    specifier="press key KEY_Enter"
)

#--------------------------------------------------------------------#
# Screen after scanning "CHOOSE AN ACTION" pop-up
# Three options for this: Scan the next page, Finish the job
#--------------------------------------------------------------------#
locator = screen.add_locator("Choose An Action")
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.PRESS,
    specifier="h-select:option_count=2"
)

locator = screen.add_locator("Content Type Value")
locator.area = "list:index=2"
locator.widget = "text='%s' && type='ListItem'"
locator.add_action(
    action=Action.PRESS,
    specifier="h-select:option_count=4"
)

locator = screen.add_locator("Content Source Value")
locator.area = "list:index=2"
locator.widget = "text='%s' && type='ListItem'"
locator.add_action(
    action=Action.PRESS,
    specifier="h-select:option_count=8"
)

locator = screen.add_locator("Background Detection Value")
locator.area = "list:index=2"
locator.widget = "text='%s' && type='ListItem'"
locator.add_action(
    action=Action.PRESS,
    specifier="h-select:option_count=2"
)

locator = screen.add_locator("List Item 2 Value")
locator.area = "list:index=2"
locator.widget = "text='%s' && type='ListItem'"
locator.add_action(
    action=Action.PRESS,
    specifier="h-select:option_count=2"
)

locator = screen.add_locator("List Item 4 Value")
locator.area = "list:index=2"
locator.widget = "text='%s' && type='ListItem'"
locator.add_action(
    action=Action.PRESS,
    specifier="h-select:option_count=4"
)

locator = screen.add_locator("List Item 5 Value")
locator.area = "list:index=2"
locator.widget = "text='%s' && type='ListItem'"
locator.add_action(
    action=Action.PRESS,
    specifier="h-select:option_count=5"
)

locator = screen.add_locator("List Item 6 Value")
locator.area = "list:index=2"
locator.widget = "text='%s' && type='ListItem'"
locator.add_action(
    action=Action.PRESS,
    specifier="h-select:option_count=6"
)

locator = screen.add_locator("Scale Value")
locator.area = "list:index=2"
locator.widget = "helpText='Adjust copy scale.'"
locator.add_action(
    action=Action.SELECT,
    specifier="h-select:data_type=int:set settingValue='%s'"
)