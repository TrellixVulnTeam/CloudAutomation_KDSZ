""" home.settings.email.emailsetup
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

screen = Screen()

locator = screen.add_locator("Primary SMTP Gateway", marker=True)
locator.area = "list"
locator.widget = "textid='66145'"

locator = screen.add_locator("Line Edit Text")
locator.widget = "type='EditBox'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify value='%s'"
)
