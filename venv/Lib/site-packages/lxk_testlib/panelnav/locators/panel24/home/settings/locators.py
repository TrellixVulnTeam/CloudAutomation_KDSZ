""" home.settings
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen
from . import (
    networkports,
    email,
    device,
    copy
)

screen = Screen()

locator = screen.add_locator("Device")
locator.area = "list"
locator.widget = "textid='72728'"
locator.next_screen = device

locator = screen.add_locator("Network/Ports")
locator.area = "list"
locator.widget = "textid='67885'"
locator.next_screen = networkports

locator = screen.add_locator("E-mail")
locator.area = "list"
locator.widget = "textid='65740'"
locator.next_screen = email

locator = screen.add_locator("Copy")
locator.area = "list"
locator.widget = "textid='65977'"
locator.next_screen = copy

#-----------------------------------------------------------------------------#
#SETTINGS SCREEN LOCATORS - OPTIMIZED/MINIMIZED VERSION
#-----------------------------------------------------------------------------#
locator = screen.add_locator("Screen Title", marker=True)
#locator.area = "objects:index=1"
locator.widget = "type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Screen Title With Index")
locator.area = "objects:index=1"
locator.widget = "type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--- viewname=BasicCompositeView
#locator = screen.add_locator("Menu List Title") - see Screen Title
locator = screen.add_locator("Menu List")
locator.area = "objects:index=2"
locator.widget = "type='ListItem'"

locator = screen.add_locator("Menu List Item")
locator.area = "objects:index=2"
locator.widget = "text='%s' && type='ListItem'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

#--- viewname=BasicSettingView
locator = screen.add_locator("Popup List")
locator.area = "objects:index=2"
locator.widget = "type='ListItem'"

locator = screen.add_locator("Popup List Title")
locator.widget = "type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Popup List Item")
locator.area = "objects:index=2"
locator.widget = "text='%s' && type='ListItem'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

#--- viewname=SetupPromptView
#locator = screen.add_locator("Prompt View Title") - see Screen Title
locator = screen.add_locator("Prompt View")
locator.area = "objects:index=2"
locator.widget = "type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Prompt View Message")
locator.area = "objects:index=2"
locator.widget = "text='%s' && type='Label'"

locator = screen.add_locator("Prompt View Left Button")
locator.area = "objects:index=3"
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.PRESS,
    specifier="h-select:option_count=2"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"

)

locator = screen.add_locator("Prompt View Right Button")
locator.area = "objects:index=4"
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.PRESS,
    specifier="h-select:option_count=2"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

#--- viewname=TextPopupView
locator = screen.add_locator("Popup Text Message")
locator.widget = "text='%s' && type='Label'"

#--- viewname=SetupStringInputView
#locator = screen.add_locator("Keyboard Edit Title") - see Screen Title

locator = screen.add_locator("Keyboard Edit")
locator.widget = "type='EditBox'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify value='%s'"
)

locator = screen.add_locator("Keyboard HelperText")
locator.area = "objects:index=4"
locator.widget = "type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--- viewname=SettingSpinnerView
#locator = screen.add_locator("Number Selector Title") - see Screen Title

locator = screen.add_locator("Number Selector")
locator.area = "objects:index=2"
locator.widget = "type='Label'"
locator.add_action(
    action=Action.PRESS,
    specifier="h-select:data_type=int:menu_entry=false:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("String Selector")
locator.area = "objects:index=2"
locator.widget = "type='Label'"
locator.add_action(
    action=Action.PRESS,
    specifier="h-select:option_count=%s:menu_entry=false:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

## Manual Date and Time
locator = screen.add_locator("Manual Month")
locator.area = "objects:index=4"
locator.widget = "type='Label'"
locator.add_action(
    action=Action.PRESS,
    specifier="v-select:data_type=int:menu_entry=false:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Day")
locator.area = "objects:index=7"
locator.widget = "type='Label'"
locator.add_action(
    action=Action.PRESS,
    specifier="v-select:data_type=int:menu_entry=false:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Year")
locator.area = "objects:index=9"
locator.widget = "type='Label'"
locator.add_action(
    action=Action.PRESS,
    specifier="v-select:data_type=int:menu_entry=false:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Hour")
locator.area = "objects:index=4"
locator.widget = "type='Label'"
locator.add_action(
    action=Action.PRESS,
    specifier="v-select:data_type=int:menu_entry=false:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Minute")
locator.area = "objects:index=7"
locator.widget = "type='Label'"
locator.add_action(
    action=Action.PRESS,
    specifier="v-select:data_type=int:menu_entry=false:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Period")
locator.area = "objects:index=9"
locator.widget = "type='Label'"
locator.add_action(
    action=Action.PRESS,
    specifier="v-select:option_count=4:menu_entry=false:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)
#-----------------------------------------------------------------------------#
#SETTINGS SCREEN LOCATORS - OPTIMIZED/MINIMIZED VERSION - END OF LOCATORS
#-----------------------------------------------------------------------------#
