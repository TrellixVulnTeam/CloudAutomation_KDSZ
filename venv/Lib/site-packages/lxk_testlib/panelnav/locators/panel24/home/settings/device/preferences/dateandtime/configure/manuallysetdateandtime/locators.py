""" home.settings.device.preferences.dateandtime.configure.manuallysetdateandtime
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

screen = Screen()

locator = screen.add_locator("Month Label", marker=True)
locator.area = "objects:index=4"
locator.widget = "Label"
locator.add_action(
    action=Action.SELECT,
    specifier="v-select:data_type=int:menu_entry=false:set text='%s'"
)

locator = screen.add_locator("Day Label")
locator.area = "objects:index=7"
locator.widget = "Label"
locator.add_action(
    action=Action.SELECT,
    specifier="v-select:data_type=int:menu_entry=false:set text='%s'"
)

locator = screen.add_locator("Year Label")
locator.area = "objects:index=9"
locator.widget = "Label"
locator.add_action(
    action=Action.SELECT,
    specifier="v-select:data_type=int:menu_entry=false:set text='%s'"
)