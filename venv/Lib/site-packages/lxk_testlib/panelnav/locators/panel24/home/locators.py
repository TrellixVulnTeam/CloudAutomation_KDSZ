""" home
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen
from . import (
    settings,
    copy,
    changelanguage,
    statussupplies,
    shortcuts,
    usbdrive,
    bookmarks,
    heldjobs
)

screen = Screen()

locator = screen.add_locator("Copy", marker=True)
locator.area = "list"
locator.widget = "textid='65977'"
locator.next_screen = copy

locator = screen.add_locator("Shortcuts")
locator.widget = "textid='67977'"
locator.next_screen = shortcuts

locator = screen.add_locator("Settings")
locator.area = "list"
locator.widget = "textid='67984'"
locator.next_screen = settings

locator = screen.add_locator("Status/Supplies")
locator.widget = "textid='68422'"
locator.next_screen = statussupplies

locator = screen.add_locator("Change Language")
locator.widget = "textid='69818'"
locator.next_screen = changelanguage

locator = screen.add_locator("USB Drive")
locator.widget = "textid='69999'"
locator.next_screen = usbdrive

locator = screen.add_locator("Bookmarks")
locator.widget = "textid='66483'"
locator.next_screen = bookmarks

locator = screen.add_locator("Held Jobs")
locator.widget = "textid='70464'"
locator.next_screen = heldjobs

locator = screen.add_locator("Cancel Job")
locator.widget = "textid='70531'"

#--------------------------------------------------------------------#
# INITIAL SET UP WIZARD LOCATORS
#--------------------------------------------------------------------#
locator = screen.add_locator("Setup Screen Title")
locator.text = "%s"
locator.widget = "type='Label'"

locator = screen.add_locator("Setup Menu List Item")
locator.widget = "text='%s' && type='ListItem'"

locator = screen.add_locator("Setup Menu List Value")
locator.widget = "text='%s' && type='ListItem'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Setup Menu Label")
locator.widget = "text='%s' && type='Label'"

locator = screen.add_locator("Setup Menu EditBox Value")
locator.widget = "type='EditBox'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify value='%s'"
)

locator = screen.add_locator("Setup Helper Text Value")
locator.text = "%s"
locator.widget = "type='Label'"

locator = screen.add_locator("Setup Message")
locator.widget = "type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

## Checkbox
locator = screen.add_locator("Setup Checkbox")
locator.widget = "type='ListItem'"
locator.text = "%s"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

## Manual Date and Time
locator = screen.add_locator("Manual Month")
locator.area = "objects:index=4"
locator.widget = "type='Label'"
locator.add_action(
    action=Action.PRESS,
    specifier="v-select:data_type=int:menu_entry=false:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Day")
locator.area = "objects:index=7"
locator.widget = "type='Label'"
locator.add_action(
    action=Action.PRESS,
    specifier="v-select:data_type=int:menu_entry=false:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Year")
locator.area = "objects:index=9"
locator.widget = "type='Label'"
locator.add_action(
    action=Action.PRESS,
    specifier="v-select:data_type=int:menu_entry=false:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Hour")
locator.area = "objects:index=4"
locator.widget = "type='Label'"
locator.add_action(
    action=Action.PRESS,
    specifier="v-select:data_type=int:menu_entry=false:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Minute")
locator.area = "objects:index=7"
locator.widget = "type='Label'"
locator.add_action(
    action=Action.PRESS,
    specifier="v-select:data_type=int:menu_entry=false:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Period")
locator.area = "objects:index=9"
locator.widget = "type='Label'"
locator.add_action(
    action=Action.PRESS,
    specifier="v-select:option_count=4:menu_entry=false:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)


locator = screen.add_locator("Choose An Action")
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.PRESS,
    specifier="h-select:option_count=2"
)

#-----------------------------------------------------------------------------#
# Notification Area Locators / Display Info
#-----------------------------------------------------------------------------#
locator = screen.add_locator("Screen Display Info")
locator.area = "objects:index=2"
locator.widget = "type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Notification Display Info Value")
locator.area = "objects:index=2"
locator.widget = "text='%s' && type='Label'"

#-----------------------------------------------------------------------------#
# IR Screen Locators
#-----------------------------------------------------------------------------#
locator = screen.add_locator("IR Screen Title")
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

locator = screen.add_locator("IR Screen Code")
locator.widget = "name='_irNumber' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

locator = screen.add_locator("IR Screen Button")
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)
locator.add_action(
    action=Action.PRESS,
    specifier="h-select:option_count=2"
)

locator = screen.add_locator("IR Screen Video Step")
locator.widget = "name='' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

locator = screen.add_locator("IR Notication Title")
locator.area = "objects:index=1"
locator.widget = "type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

locator = screen.add_locator("IR Screen List")
locator.area = "list"
locator.widget = "text='%s' && type='ListItem'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

locator = screen.add_locator("IR Home Display Info")
locator.area = "objects:index=2"
locator.widget = "text='%s' && type='Label'"