""" Locators Module
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

screen = Screen()

locator = screen.add_locator("On")
locator.widget = "textid='67897'"

locator = screen.add_locator("Off")
locator.widget = "textid='67893'"

locator = screen.add_locator("Auto")
locator.widget = "textid='65964'"

locator = screen.add_locator("ON_SELECTED")
locator.widget = "textid='67897'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("OFF_SELECTED")
locator.widget = "textid='67893'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("AUTO_SELECTED")
locator.widget = "textid='65964'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)