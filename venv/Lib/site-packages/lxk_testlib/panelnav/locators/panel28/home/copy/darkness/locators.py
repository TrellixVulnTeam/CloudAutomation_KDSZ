""" home.copy.darkness
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

screen = Screen()

locator = screen.add_locator("Darkness", marker=True)
locator.widget = "textid='65965'"

locator = screen.add_locator("Darkness Current Value")
locator.widget = "type='QLabel'"
locator.area = "x='68' && y='176'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Darkness Increment")
locator.widget = "name='scrollDown' && type='ImageButton'"

locator = screen.add_locator("Darkness Decrement")
locator.widget = "name='scrollUp' && type='ImageButton'"
