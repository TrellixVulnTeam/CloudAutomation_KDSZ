""" home.email
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

# Add screen and components below
screen = Screen()

#--------------------------------------------------------------------#
#E-MAIL SCREEN LOCATORS - OPTIMIZED/MINIMIZED VERSION
#--------------------------------------------------------------------#
locator = screen.add_locator("Screen Title", marker=True)
locator.widget = "x='0' && y='0' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Shortcut (*) and Add Button (+)
#--------------------------------------------------------------------#
locator = screen.add_locator("Shortcut Button")
locator.widget = "name='titleBarRightButton' && type='ImageButton'"

locator = screen.add_locator("Add Shortcut Button")
locator.widget = "name='titleBarRightButton' && type='ImageButton'"
#--------------------------------------------------------------------#
# Settings that uses this locator: [To:] and [Subject:]
#--------------------------------------------------------------------#
locator = screen.add_locator("Destination Field")
locator.widget = "textid='66110' && type='Label'"

locator = screen.add_locator("Subject Field")
locator.widget = "textid='70052' && type='Label'"

#--------------------------------------------------------------------#
# Settings that uses this locator: Color, Original Size, Resolution,
# Send as, PDF Settings, Darkness, Content Type, Transmission Log,
# Scan Edge to Edge, Background Removal, Contrast, Clear All Values
#--------------------------------------------------------------------#
locator = screen.add_locator("Menu List Item")
locator.widget = "text='%s' && type='Label'"

#--------------------------------------------------------------------#
# Settings that uses this locator: All Popup Dialog Settings
#--------------------------------------------------------------------#
locator = screen.add_locator("Popup List Title")
locator.widget = "x='8' && y='9' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Popup List Value")
locator.area = "ListWidget"
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Popup Dialog Title")
locator.widget = "x='8' && y='9' && type='Label'"
locator.text = "%s"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Popup Dialog Message")
locator.widget = "x='8' && y='36' && type='Label'"
locator.text = "%s"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Popup Dialog Button")
locator.widget = "text='%s' && type='QPushButton'"

locator = screen.add_locator("Dialog List Selector")
locator.area = "ListWidget"
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Darkness, Content Type,
# Background Removal, Contrast (Manual)
# FYI for slider equivalent:
# ScrollUp Button - the decrement button (-)
# ScrollDown Button - the increment button (+)
#--------------------------------------------------------------------#
locator = screen.add_locator("ScrollUp Button")
locator.widget = "name='scrollUp' && type='ImageButton'"

locator = screen.add_locator("ScrollDown Button")
locator.widget = "name='scrollDown' && type='ImageButton'"

#--------------------------------------------------------------------#
# Settings that uses this locator: Content Type and
# Button after the e-mail address <email@domain.com> | (x)
#--------------------------------------------------------------------#
locator = screen.add_locator("Text Button")
locator.widget = "text='%s' && type='QLabel'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Darkness, Background Removal,
# Contrast (Manual)
#--------------------------------------------------------------------#
locator = screen.add_locator("Current Value")
locator.widget = "type='QLabel'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: All Keyboard Window
#--------------------------------------------------------------------#
locator = screen.add_locator("Keyboard Edit")
locator.widget = "type='LineEdit'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Keyboard Button")
locator.widget = "text='%s' && type='KeyboardButton'"

locator = screen.add_locator("Keyboard Button Suggestion")
locator.widget = "text='%s' && type='QPushButton'"

locator = screen.add_locator("Add Recipient Button")
locator.widget = "text='Add' && type='KeyboardButton'"

locator = screen.add_locator("Keyboard Button Done")
locator.widget = "text='Done' && type='KeyboardButton'"

locator = screen.add_locator("Keyboard Button Shift")
locator.widget = "name='SHIFT' && type='KeyboardButton'"

locator = screen.add_locator("Keyboard Button Backspace")
locator.widget = "name='BACKSPACE' && type='KeyboardButton'"
locator.add_action(
    action=Action.PRESS,
    specifier="press:time_sec=%s"
)

locator = screen.add_locator("Hold Keyboard Button")
locator.widget = "text='%s' && type='KeyboardButton'"
locator.add_action(
    action=Action.PRESS,
    specifier="press:time_sec=1"
)

locator = screen.add_locator("Helper Text Value")
locator.widget = "type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Screen after scanning "CHOOSE AN ACTION" pop-up
# Three options for this: Cancel Job, Scan the next page, Finish the job
#--------------------------------------------------------------------#
locator = screen.add_locator("Choose An Action")
locator.widget = "text='%s' && type='QLabel'"


#--------------------------------------------------------------------#
# Spacebar Location per language:
# English/non-DBCS/Korean : x=128 y=191
# Japanese : x=250/4 y=122/203
# SimChi : x=128 y=195/197/201
# TradChi : x=120 y=187/202/208
#--------------------------------------------------------------------#
locator = screen.add_locator("Spacebar Button")
locator.widget = "(x='128' or x='250' or x='120' or x='4') && (y='187' or y='190' or y='191' or y='195' or y='197' or y='199' or y='122' or y='201' or y='202' or y='203' or y='208') && type='KeyboardButton'"

locator = screen.add_locator("Spacebar Button Value")
locator.widget = "(x='128' or x='250' or x='120' or x='4') && (y='187' or y='190' or y='191' or y='195' or y='197' or y='199' or y='122' or y='201' or y='202' or y='203' or y='208') && type='KeyboardButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Hold Spacebar Button")
locator.widget = "(x='128' or x='250' or x='120' or x='4') && (y='187' or y='190' or y='191' or y='195' or y='197' or y='199' or y='122' or y='201' or y='202' or y='203' or y='208') && type='KeyboardButton'"
locator.add_action(
    action=Action.PRESS,
    specifier="press:time_sec=1"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Color (special case since 2 labels)
#--------------------------------------------------------------------#
locator = screen.add_locator("Color Menu List Item")
locator.widget = "x='40' && y='160' && text='Color'"

locator = screen.add_locator("Color Popup List Value")
locator.widget = "x='16' && y='132' && text='Color'"

locator = screen.add_locator("Gray Popup List Value")
locator.widget = "x='16' && y='84' && text='Gray'"

locator = screen.add_locator("Black and White Popup List Value")
locator.widget = "x='16' && y='36' && text='Black and White'"

#--------------------------------------------------------------------#
#E-MAIL SCREEN LOCATORS - OPTIMIZED/MINIMIZED VERSION
#--------------------------------------------------------------------#
