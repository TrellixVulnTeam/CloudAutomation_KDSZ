""" home.settings.layout.blankpages
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

# Add screen and components below
screen = Screen()

locator = screen.add_locator("BLANK PAGES", marker=True)
locator.area = "ListWidget"
locator.widget = "textid='65771'"

locator = screen.add_locator("OPTIONS LIST")
locator.widget = "ListWidget"

locator = screen.add_locator("Print")
locator.area = "ListWidget"
locator.widget = "textid='66378'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Do Not Print")
locator.area = "ListWidget"
locator.widget = "textid='67232'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)