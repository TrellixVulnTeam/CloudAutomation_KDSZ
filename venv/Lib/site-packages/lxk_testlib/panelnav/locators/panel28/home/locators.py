""" home
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen
from . import (
    cloudconnector,
    copy,
    diagnostics,
    email,
    fax,
    jobqueue,
    semenu,
    settings,
    statussupplies,
    usbdrive,
    changelanguage,
    bookmarks,
    notificationcenter,
    heldjobs,
    shortcuts,
    scanprofiles
)

screen = Screen()

locator = screen.add_locator("Copy", marker=True)
locator.widget = "textid='71855'"
locator.next_screen = copy

locator = screen.add_locator("E-mail")
locator.widget = "textid='65740'"
locator.next_screen = email

locator = screen.add_locator("Fax")
locator.widget = "textid='71856'"
locator.next_screen = fax

locator = screen.add_locator("Shortcuts")
locator.widget = "textid='67977'"
locator.next_screen = shortcuts

locator = screen.add_locator("Cloud Connector")
locator.widget = "textid='75752'"
locator.next_screen = cloudconnector

locator = screen.add_locator("Status/Supplies")
locator.widget = "textid='68422'"
locator.next_screen = statussupplies

locator = screen.add_locator("Job Queue")
locator.widget = "textid='73042'"
locator.next_screen = jobqueue

locator = screen.add_locator("Settings")
locator.widget = "textid='67984'"
locator.next_screen = settings

locator = screen.add_locator("USB Drive")
locator.widget = "textid='66070'"
locator.next_screen = usbdrive

locator = screen.add_locator("Diagnostics")
locator.next_screen = diagnostics
locator.add_action(
    action=Action.PRESS_KEY_COMBINATION,
    specifier="KEY_Cancel, KEY_Cancel, KEY_Start, KEY_Start"
)

locator = screen.add_locator("SE Menu")
locator.next_screen = semenu
locator.add_action(
    action=Action.PRESS_KEY_COMBINATION,
    specifier="KEY_Cancel, KEY_Cancel, KEY_home, KEY_home"
)

locator = screen.add_locator("Change Language")
locator.widget = "textid='65554'"
locator.next_screen = changelanguage

locator = screen.add_locator("Bookmarks")
locator.widget = "textid='66483'"
locator.next_screen = bookmarks

locator = screen.add_locator("Notification Center")
locator.widget = "x='280' && y='8' && type='ImageWidget'"
locator.next_screen = notificationcenter

locator = screen.add_locator("Held Jobs")
locator.widget = "textid='66990'"
locator.next_screen = heldjobs

locator = screen.add_locator("Scan Profiles")
locator.widget = "textid='73878'"
locator.next_screen = scanprofiles

locator = screen.add_locator("Cancel Job")
locator.widget = "textid='70090'"

#--------------------------------------------------------------------#
# INITIAL SET UP WIZARD LOCATORS
#--------------------------------------------------------------------#
locator = screen.add_locator("Setup Screen Title")
locator.widget = "x='0' && y='0' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)
locator = screen.add_locator("Setup Menu List Item")
locator.area = "ListWidget"
locator.widget = "text='%s' && type='Label'"

locator = screen.add_locator("Setup Menu List Value")
locator.area = "ListWidget"
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Setup Helper Text Value")
locator.text = "%s"
locator.widget = "type='Label'"

locator = screen.add_locator("Setup Button")
locator.widget = "text='%s' && type='QPushButton'"

## Keyboard and Image button
locator = screen.add_locator("Keyboard Button")
locator.widget = "text='%s' && type='KeyboardButton'"

locator = screen.add_locator("Image Button")
locator.widget = "text='%s' && type='ImageButton'"

### Pop-ups ####
locator = screen.add_locator("Pop-up Dialog Title")
locator.widget = "x='8' && y='9' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Pop-up Dialog Message")
locator.widget = "x='8' && y='36' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Popup List Value")
locator.area = "ListWidget"
locator.widget = "text='%s' && type='Label'"

## Checkbox
locator = screen.add_locator("Setup Checkbox")
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

## More info image (upper right on the screen)
locator = screen.add_locator("More Info")
locator.widget = "x='272' && y='1' && type='ImageButton'"

## Manual Date and Time
locator = screen.add_locator("Manual Month")
locator.widget = "name='monthLabel'"
locator.add_action(
    action=Action.SELECT,
    specifier="select:data_type=int:forward_key=monthUp:backward_key=monthDown:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Day")
locator.widget = "name='dayLabel'"
locator.add_action(
    action=Action.SELECT,
    specifier="select:data_type=int:forward_key=dayUp:backward_key=dayDown:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Year")
locator.widget = "name='yearLabel'"
locator.add_action(
    action=Action.SELECT,
    specifier="select:data_type=int:forward_key=yearUp:backward_key=yearDown:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Hour")
locator.widget = "name='hourLabel'"
locator.add_action(
    action=Action.SELECT,
    specifier="select:data_type=int:forward_key=hourUp:backward_key=hourDown:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Minute")
locator.widget = "name='minuteLabel'"
locator.add_action(
    action=Action.SELECT,
    specifier="select:data_type=int:forward_key=minuteUp:backward_key=minuteDown:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Period")
locator.widget = "name='periodLabel'"
locator.add_action(
    action=Action.SELECT,
    specifier="select:option_count=3:forward_key=periodUp:backward_key=periodDown:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: All Keyboard Window
#--------------------------------------------------------------------#
locator = screen.add_locator("Keyboard Edit")
locator.widget = "type='LineEdit'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Keyboard Button")
locator.widget = "text='%s' && type='KeyboardButton'"

locator = screen.add_locator("Keyboard Button Suggestion")
locator.widget = "text='%s' && type='QPushButton'"

locator = screen.add_locator("Add Recipient Button")
locator.widget = "text='Add' && type='KeyboardButton'"

locator = screen.add_locator("Keyboard Button Done")
locator.widget = "text='Add' && type='KeyboardButton'"

locator = screen.add_locator("Keyboard Button Shift")
locator.widget = "name='SHIFT' && type='KeyboardButton'"

locator = screen.add_locator("Keyboard Button Backspace")
locator.widget = "name='BACKSPACE' && type='KeyboardButton'"
locator.add_action(
    action=Action.PRESS,
    specifier="press:time_sec=%s"
)

locator = screen.add_locator("Hold Keyboard Button")
locator.widget = "text='%s' && type='KeyboardButton'"
locator.add_action(
    action=Action.PRESS,
    specifier="press:time_sec=1"
)

#--------------------------------------------------------------------#
# Spacebar Location per language:
# English/non-DBCS/Korean : x=128 y=191
# Japanese : x=250/4 y=122/203
# SimChi : x=128 y=195/197/201
# TradChi : x=120 y=187/202/208
#--------------------------------------------------------------------#
locator = screen.add_locator("Spacebar Button")
locator.widget = "(x='128' or x='250' or x='120' or x='4') && (y='187' or y='190' or y='191' or y='195' or y='197' or y='199' or y='122' or y='201' or y='202' or y='203' or y='208') && type='KeyboardButton'"

locator = screen.add_locator("Spacebar Button Value")
locator.widget = "(x='128' or x='250' or x='120' or x='4') && (y='187' or y='190' or y='191' or y='195' or y='197' or y='199' or y='122' or y='201' or y='202' or y='203' or y='208') && type='KeyboardButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Hold Spacebar Button")
locator.widget = "(x='128' or x='250' or x='120' or x='4') && (y='187' or y='190' or y='191' or y='195' or y='197' or y='199' or y='122' or y='201' or y='202' or y='203' or y='208') && type='KeyboardButton'"
locator.add_action(
    action=Action.PRESS,
    specifier="press:time_sec=1"
)

locator = screen.add_locator("Dialog List Selector")
locator.area = "ListWidget"
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Popup Dialog Button")
locator.widget = "text='%s' && type='QPushButton'"

#-----------------------------------------------------------------------------#
# Notification Area Locators #
#-----------------------------------------------------------------------------#
locator = screen.add_locator("Screen Display Info")
#locator.area = "objects:index=1"
locator.widget = "x='280' && y='8'&& type='ImageWidget'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)
#Note: locator.widget = "x='280' && y='8'&& type='ImageWidget'" is temporary since there is a bug reported
#      during creation of script.

locator = screen.add_locator("Notification Drawer")
locator.area = "objects:index=1"
locator.widget = "text='%s' && type='Label'"

locator = screen.add_locator("Notification Display Info Value")
locator.area = "ListWidget"
locator.widget = "text='%s' && type='Label'"

#-----------------------------------------------------------------------------#
# IR Screen Locators #
#-----------------------------------------------------------------------------#
locator = screen.add_locator("IR Screen Title")
locator.widget = "x='8' && y ='1' && height='34' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

locator = screen.add_locator("IR Screen Code")
locator.widget = "x='8' && y ='36' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

locator = screen.add_locator("IR Screen Steps")
locator.widget = "x='8' && y ='70' && textid='69572' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

locator = screen.add_locator("IR Screen Button")
locator.widget = "text='%s' && type='QPushButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

locator = screen.add_locator("IR Screen Video Next Button")
locator.widget = "name='moreInfoNextButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

locator = screen.add_locator("IR Screen Video Previous Button")
locator.widget = "name='moreInfoPreviousButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

locator = screen.add_locator("IR Screen Video Done Button")
locator.widget = "name='moreInfoDoneButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

locator = screen.add_locator("IR Screen Video Step")
locator.widget = "textid='69572' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

#-----------------------------------------------------------------------------#
locator = screen.add_locator("IR Notification Center")
locator.widget = "x='280' && y='8' && type='ImageWidget'"

#Same with Status/Supplies Label
locator = screen.add_locator("IR Notification Center Label")
locator.widget = "x='0' && y='0'&& height='36' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)
#Same with Status/Supplies List
locator = screen.add_locator("IR Notification Center List")
locator.area = "ListWidget"
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

locator = screen.add_locator("IR Notification Center Supplies Button")
locator.widget = "type='ImageButton' && name='leftWellButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

locator = screen.add_locator("IR Notification Center Job Queue Button")
locator.widget = "type='ImageButton' && name='rightWellButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)


