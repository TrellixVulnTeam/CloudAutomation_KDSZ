""" home.settings.networkports.networkoverview.scantopcportrange
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

screen = Screen()

locator = screen.add_locator("SCAN TO PC PORT RANGE VALUE")
locator.widget = "type='LineEdit'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("OK")
locator.widget = "text='OK'"