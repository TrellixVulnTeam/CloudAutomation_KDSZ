""" Locators Module
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

# Add screen and components below
screen = Screen()

locator = screen.add_locator("COLOR_HEADER")
locator.area = "textid='68453':ascend=2"
locator.widget = "textid='65938'"

locator = screen.add_locator("BLACK_AND_WHITE_SELECTED")
locator.widget = "x='8' && y='44' && type='OptionButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("GRAY_SELECTED")
locator.widget = "x='138' && y='44' && type='OptionButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("COLOR_SELECTED")
locator.widget = "x='8' && y='84' && type='OptionButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("AUTO_SELECTED")
locator.widget = "x='138' && y='84' && type='OptionButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)