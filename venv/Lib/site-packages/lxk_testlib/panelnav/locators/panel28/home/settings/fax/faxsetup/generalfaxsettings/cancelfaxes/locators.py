""" home.settings.fax.faxsetup.generalfaxsettings.cancelfaxes
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen


# Add screen and components below

screen = Screen()

locator = screen.add_locator("Allow", marker=True)
locator.widget = "textid='66291'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'")

locator = screen.add_locator("Don't Allow")
locator.widget = "textid='66292'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'")