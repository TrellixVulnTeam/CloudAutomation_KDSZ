""" home.settings.layout.flipstyle
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

# Add screen and components below
screen = Screen()

locator = screen.add_locator("FLIP STYLE", marker=True)
locator.area = "ListWidget"
locator.widget = "textid='75328'"

locator = screen.add_locator("OPTIONS LIST")
locator.widget = "ListWidget"

locator = screen.add_locator("Long Edge")
locator.area = "ListWidget"
locator.widget = "textid='67206'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Short Edge")
locator.area = "ListWidget"
locator.widget = "textid='66234'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)