""" panel28.home.usbdrive
"""
from lxk_testlib.panelnav.core.models.screen import Screen
from lxk_testlib.panelnav.core.enum import Action, Filter

screen = Screen()

#Screen Title
locator = screen.add_locator("Screen Title", marker=True)
locator.widget = "type='Label' && y='0'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#Screen Title Back Buttton
locator = screen.add_locator("Screen Title Back Buttton")
locator.widget = "name='titleBarLeftButton' && x='0' && y='1'"

#Menu List - All List
locator = screen.add_locator("Menu List")
locator.widget = "type='ListWidget'"

#Menu List Item - All Main Option
locator = screen.add_locator("Menu List Item")
locator.area = "type='ListWidget'"
locator.widget = "text='%s' && type='Label' && (height='22' or height='48')"

#Menu List Item Secondary Text - All Text under Main Option
locator = screen.add_locator("Menu List Item Secondary Text")
locator.area = "type='ListWidget'"
locator.widget = "text='%s' && type='Label' && height='19'"

#Menu List Item Button
locator = screen.add_locator("Menu List Item Button")
locator.area = "type='ListWidget'"
locator.widget = "text='%s' && type='QPushButton'"

#Popup List Title
locator = screen.add_locator("Popup List Title")
locator.area = "type='ListWidget'"
locator.widget = "type='Label' && x='8' && y='9'"

#Popup List Item
locator = screen.add_locator("Popup List Item")
locator.area = "type='ListWidget'"
locator.widget = "text='%s' && type='Label'"

#Input Box
locator = screen.add_locator("Input Box")
locator.widget = "type='LineEdit'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#Keyboard Button
locator = screen.add_locator("Keyboard Button")
locator.widget = "text='%s' && type='KeyboardButton'"

#Numpad
locator = screen.add_locator("Numpad")
locator.widget = "type='LineEdit'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#Numpad Button
locator = screen.add_locator("Numpad Button")
locator.widget = "text='%s' && type='ImageButton'"

#Image List Title
#For viewname=OptionsWithIconsView, OptionsWithCarouselView, IconStepperView, OptionsWithCarouselView
locator = screen.add_locator("Image List Title")
locator.widget = "type='Label' && x='0' && y='0'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#Image List Item
#For viewname=OptionsWithIconsView, OptionsWithCarouselView, IconStepperView, OptionsWithCarouselView
locator = screen.add_locator("Image List Item")
locator.widget = "text='%s' && type='QLabel'"

#Color Balance Title
#For viewname=ColorBalanceView 
locator = screen.add_locator("Color Balance Title")
locator.widget = "type='Label' && x='0' && y='0'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#Color Balance Slider
#For viewname=ColorBalanceView 
locator = screen.add_locator("Color Balance Slider")
locator.widget = "type='Label' && x='132'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)
locator.add_action(
    action=Action.SELECT,
    specifier="select:option_count=10:set text='%s'"
)

#Number of Copies
locator = screen.add_locator("Number of Copies")
locator.area = "type='ListWidget'"
locator.widget = "name='numericButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)
locator.add_action(
    action=Action.SELECT,
    specifier="select:data_type=int:forward_key=incrementButton:backward_key=decrementButton:set text='%s'"
)

#Dialog Title
locator = screen.add_locator("Dialog Title")
locator.widget = "type='Label' && x='8' && y='9'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#Dialog Message
locator = screen.add_locator("Dialog Message")
locator.widget = "type='Label' && x='8' && y='36'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#Dialog Button
locator = screen.add_locator("Dialog Button")
locator.widget = "text='%s' && type='QPushButton'"

#--------------------------------------------------------------------#
# Screen after scanning "CHOOSE AN ACTION" pop-up
# Three options for this: Cancel Job, Scan the next page, Finish the job
#--------------------------------------------------------------------#
locator = screen.add_locator("Choose An Action")
locator.widget = "text='%s' && type='QLabel'"
