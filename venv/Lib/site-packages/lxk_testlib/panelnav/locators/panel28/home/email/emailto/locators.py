""" home.email.emailto
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

screen = Screen()

locator = screen.add_locator("LineEdit Value", marker=True)
locator.widget = "type='LineEdit'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Enter E-mail Helper Text Value")
locator.widget = "type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Saved E-mail Address Icon")
locator.widget = "type='ImageButton' and name='keyboardEntryIcon'"

locator = screen.add_locator("Saved E-mail Address Value")
locator.widget = "type='QLabel'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Keyboard Key")
locator.text="%s"
locator.widget = "type='KeyboardButton'"

locator = screen.add_locator("Press and Hold Keyboard Key")
locator.text="%s"
locator.widget = "type='KeyboardButton'"
locator.add_action(
    action=Action.PRESS,
    specifier="press:time_sec=1"
)

locator = screen.add_locator("Keyboard Shift Key")
locator.widget = "type='KeyboardButton' && name='SHIFT'"

locator = screen.add_locator("Keyboard Backspace Key")
locator.widget = "type='KeyboardButton' && name='BACKSPACE'"
locator.add_action(
    action=Action.PRESS,
    specifier="press:time_sec=%s"
)

#This is for Done, Add and OK -- Keyboard Keys
locator = screen.add_locator("Keyboard OK Key")
locator.text="%s"
locator.widget = "type='KeyboardButton'"

#### Spacebar ####
#Unique spacebar dimensions: TradChi, Japanese, SimChi
locator = screen.add_locator("Keyboard Spacebar Key")
locator.widget = "(x='128' or x='250' or x='120') && (y='191' or y='197' or y='122' or y='202') && type='KeyboardButton'"

locator = screen.add_locator("Press and Hold Keyboard Spacebar Key")
locator.widget = "(x='128' or x='250' or x='120') && (y='191' or y='197' or y='122' or y='202') && type='KeyboardButton'"
locator.add_action(
    action=Action.PRESS,
    specifier="press:time_sec=1"
)

locator = screen.add_locator("Keyboard Spacebar Key Value")
locator.widget = "(x='128' or x='250' or x='120') && (y='191' or y='197' or y='122' or y='202') && type='KeyboardButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)



#### Keyboard Type Pop-up ####
locator = screen.add_locator("Keyboard Type List Title")
locator.widget = "type='Label' && textid='66504'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Keyboard Type List")
locator.text="%s"
locator.widget = "type='Label'"

locator = screen.add_locator("Keyboard Type List Value")
locator.widget = "type='Label'"
locator.text="%s"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Keyboard Type Cancel")
locator.widget = "textid='65931'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Keyboard Type OK")
locator.widget = "textid='67896' or name='OK'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)
