""" Locators Module
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

screen = Screen()

###################### SLEEP MODE PROFILE SETTINGS ######################
locator = screen.add_locator("Print from Sleep Mode", marker=True)
locator.widget = "textid='76024'"

locator = screen.add_locator("Stay awake after printing")
locator.widget = "textid='76026'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Enter Sleep Mode after printing")
locator.widget = "textid='76025'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

