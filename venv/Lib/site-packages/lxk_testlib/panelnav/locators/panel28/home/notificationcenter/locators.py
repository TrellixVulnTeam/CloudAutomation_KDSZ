""" home.notificationcenter
"""
from lxk_testlib.panelnav.core.enum import Action
from lxk_testlib.panelnav.core.models.screen import Screen
from .. import statussupplies
from .. import jobqueue
from .. import settings

screen = Screen()

locator = screen.add_locator("Screen Title", marker=True)
locator.widget = "x='0' && y='0' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

### Buttons ###
locator = screen.add_locator("Status/Supplies Button")
locator.widget = "name='leftWellButton'"
locator.next_screen = statussupplies

locator = screen.add_locator("Job Queue Button")
locator.widget = "name='rightWellButton'"
locator.next_screen = jobqueue

### Notofication List ###
locator = screen.add_locator("Notification List Item")
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Network Status")
locator.widget = "text='%s' && textid='69572'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)
locator.next_screen = settings