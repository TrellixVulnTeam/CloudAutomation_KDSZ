""" home.settings.layout.printarea
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

# Add screen and components below
screen = Screen()

locator = screen.add_locator("PRINT AREA", marker=True)
locator.area = "ListWidget"
locator.widget = "textid='67221'"

locator = screen.add_locator("OPTIONS LIST")
locator.widget = "ListWidget"

locator = screen.add_locator("Normal")
locator.area = "ListWidget"
locator.widget = "textid='67891'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Fit to page")
locator.area = "ListWidget"
locator.widget = "textid='67455'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Whole Page")
locator.area = "ListWidget"
locator.widget = "textid='67222'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)
