"""panel28.home.jobqueue
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

screen = Screen()

#--------------------------------------------------------------------#
#STATUS/SUPPLIES SCREEN LOCATORS - OPTIMIZED/MINIMIZED VERSION
#--------------------------------------------------------------------#
locator = screen.add_locator("Screen Title", marker=True)
locator.widget = "x='0' && y='0' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--uses: Pause/Start ImageButton
locator = screen.add_locator("Title Bar Button")
locator.widget = "name='titleBarRightButton' && type='ImageButton'"


#--uses: Print, Network, Fax, Waiting Job Menus
locator = screen.add_locator("Menu List Item")
locator.area = "ListWidget"
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--uses: Details under Menu List Item i.e. "0 Jobs"
locator = screen.add_locator("Helper Text Value")
locator.widget = "type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--uses: Message seen on Menu Items if there is no job
locator = screen.add_locator("Screen Message")
locator.widget = "type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--uses: Job Names
locator = screen.add_locator("Job Name")
locator.widget = "type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--uses: Job Owner
locator = screen.add_locator("Job Owner")
locator.widget = "type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--uses: Job Status
locator = screen.add_locator("Job Status")
locator.widget = "type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--uses: Job Cancel Button
locator = screen.add_locator("Job Cancel Button")
locator.widget = "name='listItemButton' && type='ImageButton'"


locator = screen.add_locator("Popup Dialog Title")
locator.widget = "x='8' && y='9' && type='Label'"
locator.text = "%s"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Popup Dialog Message")
locator.widget = "x='8' && y='36' && type='Label'"
locator.text = "%s"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Popup Dialog Button")
locator.widget = "text='%s' && type='QPushButton'"