""" home.settings
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen
from . import (
    device,
    email,
    fax,
    reports,
    print
)

# Add screen and components below
screen = Screen()

locator = screen.add_locator("SETTINGS")
locator.widget = "textid='69572'"

locator = screen.add_locator("MENU LIST")
locator.widget = "ListWidget"

locator = screen.add_locator("Device")
locator.widget = "textid='72728'"
locator.next_screen = device

locator = screen.add_locator("E-mail")
locator.widget = "textid='65740'"
locator.next_screen = email

locator = screen.add_locator("Fax")
locator.widget = "textid='66046'"
locator.next_screen = fax

locator = screen.add_locator("Reports")
locator.widget = "textid='67926'"
locator.next_screen = reports

locator = screen.add_locator("Print Menu")
locator.area = "ListWidget"
locator.widget = "textid='66378'"
locator.next_screen = print

#-----------------------------------------------------------------------------#
#SETTINGS SCREEN LOCATORS - OPTIMIZED/MINIMIZED VERSION
#-----------------------------------------------------------------------------#
locator = screen.add_locator("Screen Title", marker=True)
locator.widget = "(x='0' or x='8') && (y='0' or y='9') && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Menu List Item")
locator.area = "ListWidget"
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Popup List")
locator.widget = "ListWidget"

locator = screen.add_locator("Popup List Title")
locator.widget = "x='8' && y='9' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Popup List Value")
locator.area = "ListWidget"
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Popup Dialog Title")
locator.widget = "x='8' && y='9' && type='Label'"
locator.text = "%s"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Popup Dialog Message")
locator.widget = "x='8' && y='36' && type='Label'"
locator.text = "%s"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Popup Dialog Button")
locator.widget = "text='%s' && type='QPushButton'"

locator = screen.add_locator("Dialog List Selector")
locator.area = "ListWidget"
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Option textvalue")
locator.widget = "x='8' && y='36' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: All Keyboard Window
#--------------------------------------------------------------------#
locator = screen.add_locator("Keyboard Edit")
locator.widget = "type='LineEdit'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Keyboard Button")
locator.widget = "text='%s' && type='KeyboardButton'"

locator = screen.add_locator("Keyboard Button Suggestion")
locator.widget = "text='%s' && type='QPushButton'"

locator = screen.add_locator("Keyboard Button Shift")
locator.widget = "name='SHIFT' && type='KeyboardButton'"

locator = screen.add_locator("Keyboard Button Backspace")
locator.widget = "name='BACKSPACE' && type='KeyboardButton'"
locator.add_action(
    action=Action.PRESS,
    specifier="press:time_sec=%s"
)

locator = screen.add_locator("Hold Keyboard Button")
locator.widget = "text='%s' && type='KeyboardButton'"
locator.add_action(
    action=Action.PRESS,
    specifier="press:time_sec=1"
)

locator = screen.add_locator("Helper Text Value")
locator.widget = "type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Spacebar Location per language:
# English/non-DBCS/Korean : x=128 y=191
# Japanese : x=250/4 y=122/203
# SimChi : x=128 y=195/197/201
# TradChi : x=120 y=187/202/208
#--------------------------------------------------------------------#
locator = screen.add_locator("Spacebar Button")
locator.widget = "(x='128' or x='250' or x='120' or x='4') && (y='187' or y='190' or y='191' or y='195' or y='197' or y='199' or y='122' or y='201' or y='202' or y='203' or y='208') && type='KeyboardButton'"

locator = screen.add_locator("Spacebar Button Value")
locator.widget = "(x='128' or x='250' or x='120' or x='4') && (y='187' or y='190' or y='191' or y='195' or y='197' or y='199' or y='122' or y='201' or y='202' or y='203' or y='208') && type='KeyboardButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Hold Spacebar Button")
locator.widget = "(x='128' or x='250' or x='120' or x='4') && (y='187' or y='190' or y='191' or y='195' or y='197' or y='199' or y='122' or y='201' or y='202' or y='203' or y='208') && type='KeyboardButton'"
locator.add_action(
    action=Action.PRESS,
    specifier="press:time_sec=1"
)

locator = screen.add_locator("Numpad Edit")
locator.widget = "type='LineEdit'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Numpad Button")
locator.widget = "text='%s' && type='ImageButton'"

locator = screen.add_locator("Manual Month")
locator.widget = "name='monthLabel'"
locator.add_action(
    action=Action.SELECT,
    specifier="select:data_type=int:forward_key=monthUp:backward_key=monthDown:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Day")
locator.widget = "name='dayLabel'"
locator.add_action(
    action=Action.SELECT,
    specifier="select:data_type=int:forward_key=dayUp:backward_key=dayDown:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Year")
locator.widget = "name='yearLabel'"
locator.add_action(
    action=Action.SELECT,
    specifier="select:data_type=int:forward_key=yearUp:backward_key=yearDown:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Hour")
locator.widget = "name='hourLabel'"
locator.add_action(
    action=Action.SELECT,
    specifier="select:data_type=int:forward_key=hourUp:backward_key=hourDown:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Minute")
locator.widget = "name='minuteLabel'"
locator.add_action(
    action=Action.SELECT,
    specifier="select:data_type=int:forward_key=minuteUp:backward_key=minuteDown:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Period")
locator.widget = "name='periodLabel'"
locator.add_action(
    action=Action.SELECT,
    specifier="select:option_count=3:forward_key=periodUp:backward_key=periodDown:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)
#-----------------------------------------------------------------------------#
#SETTINGS SCREEN LOCATORS - OPTIMIZED/MINIMIZED VERSION - END OF LOCATORS
#-----------------------------------------------------------------------------#
