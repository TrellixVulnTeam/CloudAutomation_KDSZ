""" home.settings.device.restorefactorydefaults
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

# Add screen and components below
screen = Screen()

locator = screen.add_locator("RESTORE_ALL_SETTINGS", marker=True)
locator.widget = "textid='72854'"
locator.add_action(
    action=Action.SELECT,
    specifier="press:set selected='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Restore printer settings")
locator.widget = "textid='72850'"

locator = screen.add_locator("Restore network settings")
locator.widget = "textid='72851'"

locator = screen.add_locator("Restore fax settings")
locator.widget = "textid='72852'"

locator = screen.add_locator("Cancel")
locator.widget = "textid='65931'"

locator = screen.add_locator("RESTORE")
locator.widget = "textid='66134'"
