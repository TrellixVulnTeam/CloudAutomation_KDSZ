""" home.copy
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

# Add screen and components below
screen = Screen()

#--------------------------------------------------------------------#
#COPY SCREEN LOCATORS - OPTIMIZED/MINIMIZED VERSION
#--------------------------------------------------------------------#

#--------------------------------------------------------------------#
# Settings that use this locator: Copy Landing Screen, Sides, Darkness,
# Content Type,Collate, Pages per Side, Background Removal, Contrast Manual
# Color Balance,Color Balance Cyan-Red, Color Balance Magenta-Green
# and Color Balance Yellow-Blue
#--------------------------------------------------------------------#
locator = screen.add_locator("Screen Title", marker=True)
locator.widget = "x='0' && y='0' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)
#--------------------------------------------------------------------#
# Setting that uses this locator: Shortcut (*)
#--------------------------------------------------------------------#
locator = screen.add_locator("Title Bar Button")
locator.widget = "name='titleBarRightButton' && type='ImageButton'"

#--------------------------------------------------------------------#
# Setting that uses this locator: Shortcut Screen and Clear All Values
#--------------------------------------------------------------------#
locator = screen.add_locator("Popup Dialog Title")
locator.widget = "x='8' && y='9' && type='Label'"
locator.text = "%s"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Setting that uses this locator: Shortcut Screen
#--------------------------------------------------------------------#
locator = screen.add_locator("Popup Dialog Button")
locator.widget = "text='%s' && type='QPushButton'"
#--------------------------------------------------------------------#
# Setting that uses this locator: Copies
#--------------------------------------------------------------------#
locator = screen.add_locator("Numeric Stepper")
locator.widget = "type='ListNumericStepper'"

#--------------------------------------------------------------------#
# Setting that uses this locator: Copies
#--------------------------------------------------------------------#
locator = screen.add_locator("Decrement Button")
locator.widget = "name='decrementButton' && type='ImageButton'"

#--------------------------------------------------------------------#
# Setting that uses this locator: Copies
#--------------------------------------------------------------------#
locator = screen.add_locator("Increment Button")
locator.widget = "name='incrementButton' && type='ImageButton'"

#--------------------------------------------------------------------#
# Setting that uses this locator: Copies
#--------------------------------------------------------------------#
locator = screen.add_locator("Numeric Stepper Value")
locator.widget = "name='numericButton' && type='QPushButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that use this locator: Color, ID Copy,Copy from, Copy to,
# Sides, Scale, Darkness, Content Type, Collate, Pages per Side,
# Scan Edge to Edge, Background Removal, Contrast, Color Balance,
# and Clear All Values
#--------------------------------------------------------------------#
locator = screen.add_locator("Menu List Item")
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that use this locator: Color, Copy from, Copy to,
# Scale and Contrast
#--------------------------------------------------------------------#
locator = screen.add_locator("Popup List Title")
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that use this locator: Color, Copy from, Copy to,
# Scale and Contrast
#--------------------------------------------------------------------#
locator = screen.add_locator("Popup List Value")
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Scale OK Button")
locator.widget = "text='OK' && type='ImageButton'"

#--------------------------------------------------------------------#
# Settings that use this locator: Sides, Darkness, Content Type,
# Pages per Side, Background Removal, Contrast Manual, Color Balance Cyan-Red,
# Color Balance Magenta-Green and Color Balance Yellow-Blue
#--------------------------------------------------------------------#
locator = screen.add_locator("ScrollUp Button")
locator.widget = "name='scrollUp' && type='ImageButton'"

locator = screen.add_locator("Option Value")
locator.text = "%s"
#--------------------------------------------------------------------#
# Settings that use this locator: Sides, Darkness, Content Type,
# Pages per Side, Background Removal, Contrast Manual, Color Balance Cyan-Red,
# Color Balance Magenta-Green and Color Balance Yellow-Blue
#--------------------------------------------------------------------#
locator = screen.add_locator("ScrollDown Button")
locator.widget = "name='scrollDown' && type='ImageButton'"

#--------------------------------------------------------------------#
# Settings that use this locator: Sides, Darkness, Content Type,
# Pages per Side, Background Removal and Contrast Manual
#--------------------------------------------------------------------#
locator = screen.add_locator("Current Value")
locator.widget = "type='QLabel'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that use this locator: Color Balance Cyan-Red,
# Color Balance Magenta-Green and Color Balance Yellow-Blue
#--------------------------------------------------------------------#
locator = screen.add_locator("Default Value")
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that use this locator: Clear All Values
#--------------------------------------------------------------------#
locator = screen.add_locator("Popup Dialog Message")
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

# Settings that uses this locator: All Keyboard Window
#--------------------------------------------------------------------#
locator = screen.add_locator("Keyboard Edit")
locator.widget = "type='LineEdit'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Keyboard Button")
locator.widget = "text='%s' && type='KeyboardButton'"

locator = screen.add_locator("Keyboard Button Shift")
locator.widget = "name='SHIFT' && type='KeyboardButton'"

locator = screen.add_locator("Keyboard Button Backspace")
locator.widget = "name='BACKSPACE' && type='KeyboardButton'"
locator.add_action(
    action=Action.PRESS,
    specifier="press:time_sec=%s"
)

locator = screen.add_locator("Hold Keyboard Button")
locator.widget = "text='%s' && type='KeyboardButton'"
locator.add_action(
    action=Action.PRESS,
    specifier="press:time_sec=1"
)

locator = screen.add_locator("Helper Text Value")
locator.widget = "type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Screen after scanning "CHOOSE AN ACTION" pop-up
# Three options for this: Cancel Job, Scan the next page, Finish the job
#--------------------------------------------------------------------#
locator = screen.add_locator("Choose An Action")
locator.widget = "text='%s' && type='QLabel'"

#--------------------------------------------------------------------#
# Spacebar Location per language:
# English/non-DBCS/Korean : x=128 y=191
# Japanese : x=10 y=196
# SimChi : x=128 y=197
# TradChi : x=120 y=202
#--------------------------------------------------------------------#
locator = screen.add_locator("Spacebar Button")
locator.widget = "(x='128' or x='10' or x='120') && (y='191' or y='196' or y='197' or y='202') && type='KeyboardButton'"

locator = screen.add_locator("Spacebar Button Value")
locator.widget = "(x='128' or x='10' or x='120') && (y='191' or y='196' or y='197' or y='202') && type='KeyboardButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Hold Spacebar Button")
locator.widget = "(x='128' or x='10' or x='120') && (y='191' or y='196' or y='197' or y='202') && type='KeyboardButton'"
locator.add_action(
    action=Action.PRESS,
    specifier="press:time_sec=1"
)

#--------------------------------------------------------------------#
#COPY SCREEN LOCATORS - OPTIMIZED/MINIMIZED VERSION
#--------------------------------------------------------------------#
