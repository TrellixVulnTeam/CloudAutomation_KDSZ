""" home.settings.layout.copies
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

# Add screen and components below
screen = Screen()

locator = screen.add_locator("Copies Input", marker=True)
locator.widget = "type='LineEdit'"

locator = screen.add_locator("Copies Value")
locator.widget = "type='LineEdit'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Copies OK")
locator.widget = "name='ok'&&type='ImageButton'"
