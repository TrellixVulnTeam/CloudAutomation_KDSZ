""" home.settings.layout.pagespersideordering
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

# Add screen and components below
screen = Screen()

locator = screen.add_locator("PAGES PER SIDE ORDERING", marker=True)
locator.area = "ListWidget"
locator.widget = "textid='68154'"

locator = screen.add_locator("OPTIONS LIST")
locator.widget = "ListWidget"

locator = screen.add_locator("Horizontal")
locator.area = "ListWidget"
locator.widget = "textid='67245'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Reverse Horizontal")
locator.area = "ListWidget"
locator.widget = "textid='67247'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Vertical")
locator.area = "ListWidget"
locator.widget = "textid='67246'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)
locator = screen.add_locator("Reverse Vertical")
locator.area = "ListWidget"
locator.widget = "textid='67248'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)
