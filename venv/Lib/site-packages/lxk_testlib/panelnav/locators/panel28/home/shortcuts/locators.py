""" home.shortcuts
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen
from lxk_testlib.panelnav.locators.panel28.home import copy

screen = Screen()

locator = screen.add_locator("Screen Title", marker=True)
locator.widget = "x='0' && y='0' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Title Bar Button")
locator.widget = "name='titleBarRightButton' && type='ImageButton'"

locator = screen.add_locator("Search Shortcut Button")
locator.widget = "name='rightWellButton' && type='ImageButton'"

locator = screen.add_locator("Numpad Shortcut Button")
locator.widget = "name='leftWellButton' && type='ImageButton'"

locator = screen.add_locator("Menu List Item")
locator.area = "ListWidget"
locator.widget = "text='%s' && type='Label'"

locator = screen.add_locator("Copy Shortcut Item")
locator.area = "ListWidget"
locator.widget = "text='%s' && type='Label'"
locator.next_screen = copy

locator = screen.add_locator("Shortcut Item")
locator.area = "ListWidget"
locator.widget = "text='%s' && type='Label'"

locator = screen.add_locator("Popup List Title")
locator.widget = "x='8' && y='9' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Popup List Value")
locator.area = "ListWidget"
locator.widget = "text='%s' && type='Label'"

locator = screen.add_locator("Popup Dialog Title")
locator.widget = "x='8' && y='9' && type='Label'"
locator.text = "%s"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Popup Dialog Message")
locator.widget = "x='8' && y='36' && type='Label'"
locator.text = "%s"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Popup Dialog Button")
locator.widget = "text='%s' && type='QPushButton'"

locator = screen.add_locator("Dialog List Selector")
locator.area = "ListWidget"
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Keyboard Edit")
locator.widget = "type='LineEdit'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Keyboard Button")
locator.widget = "text='%s' && type='KeyboardButton'"

locator = screen.add_locator("Hold Keyboard Button")
locator.widget = "name='%s' && type='KeyboardButton'"
locator.add_action(
    action=Action.PRESS,
    specifier="press:time_sec='%s'"
)

#--------------------------------------------------------------------#
# Spacebar Location per language:
# English/non-DBCS/Korean : x=128 y=191
# Japanese : x=10 y=196
# SimChi : x=128 y=197
# TradChi : x=120 y=202
#--------------------------------------------------------------------#
locator = screen.add_locator("Spacebar Button")
locator.widget = "(x='128' or x='10' or x='120') && (y='191' or y='196' or y='197' or y='202') && type='KeyboardButton'"

locator = screen.add_locator("Spacebar Button Value")
locator.widget = "(x='128' or x='10' or x='120') && (y='191' or y='196' or y='197' or y='202') && type='KeyboardButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)
locator = screen.add_locator("Hold Spacebar Button")
locator.widget = "(x='128' or x='10' or x='120') && (y='191' or y='196' or y='197' or y='202') && type='KeyboardButton'"
locator.add_action(
    action=Action.PRESS,
    specifier="press:time_sec='%s'"
)

locator = screen.add_locator("Numpad Button")
locator.widget = "text='%s' && type='ImageButton'"

locator = screen.add_locator("Helper Text Value")
locator.widget = "type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)
locator = screen.add_locator("Manual Month")
locator.widget = "name='monthLabel'"
locator.add_action(
    action=Action.SELECT,
    specifier="select:data_type=int:forward_key=monthUp:backward_key=monthDown:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Day")
locator.widget = "name='dayLabel'"
locator.add_action(
    action=Action.SELECT,
    specifier="select:data_type=int:forward_key=dayUp:backward_key=dayDown:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Year")
locator.widget = "name='yearLabel'"
locator.add_action(
    action=Action.SELECT,
    specifier="select:data_type=int:forward_key=yearUp:backward_key=yearDown:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Hour")
locator.widget = "name='hourLabel'"
locator.add_action(
    action=Action.SELECT,
    specifier="select:data_type=int:forward_key=hourUp:backward_key=hourDown:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Minute")
locator.widget = "name='minuteLabel'"
locator.add_action(
    action=Action.SELECT,
    specifier="select:data_type=int:forward_key=minuteUp:backward_key=minuteDown:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Period")
locator.widget = "name='periodLabel'"
locator.add_action(
    action=Action.SELECT,
    specifier="select:option_count=3:forward_key=periodUp:backward_key=periodDown:set text='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("ScrollUp Button")
locator.widget = "name='scrollUp' && type='ImageButton'"

locator = screen.add_locator("ScrollDown Button")
locator.widget = "name='scrollDown' && type='ImageButton'"

locator = screen.add_locator("Text Button")
locator.widget = "text='%s' && type='QLabel'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Current Value")
locator.widget = "type='QLabel'"
locator.text = '%s'
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Numeric Stepper")
locator.widget = "type='ListNumericStepper'"

locator = screen.add_locator("Decrement Button")
locator.widget = "name='decrementButton' && type='ImageButton'"

locator = screen.add_locator("Increment Button")
locator.widget = "name='incrementButton' && type='ImageButton'"

locator = screen.add_locator("Numeric Stepper Value")
locator.widget = "name='numericButton' && type='QPushButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Choose An Action")
locator.widget = "text='%s' && type='QLabel'"

#Menu List Delete Button
locator = screen.add_locator("Menu List Delete Button")
locator.widget = "type='ImageButton' && (name='titleBarRightButton' or name='titleBarRightButton2')"

#Popup List Title
locator = screen.add_locator("Popup List Title")
locator.area = "type='ListWidget'"
locator.widget = "type='Label' && x='8' && y='9'"

#Popup List Button
locator = screen.add_locator("Popup List Button")
locator.widget = "text='%s' && type='QPushButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

#Popup List Item
locator = screen.add_locator("Popup List Item")
locator.area = "type='ListWidget'"
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)
