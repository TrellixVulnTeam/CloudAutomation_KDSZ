""" home.settings.layout.pagespersideorientation
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

# Add screen and components below
screen = Screen()

locator = screen.add_locator("PAGES PER SIDE ORIENTATION", marker=True)
locator.area = "ListWidget"
locator.widget = "textid='68166'"

locator = screen.add_locator("OPTIONS LIST")
locator.widget = "ListWidget"

locator = screen.add_locator("Auto")
locator.area = "ListWidget"
locator.widget = "textid='65964'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Landscape")
locator.area = "ListWidget"
locator.widget = "textid='66025'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Portrait")
locator.area = "ListWidget"
locator.widget = "textid='66024'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

