""" home.settings.layout.pagespersideborder
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

# Add screen and components below
screen = Screen()

locator = screen.add_locator("PAGES PER SIDE BORDER", marker=True)
locator.area = "ListWidget"
locator.widget = "textid='68216'"

locator = screen.add_locator("OPTIONS LIST")
locator.widget = "ListWidget"

locator = screen.add_locator("None")
locator.area = "ListWidget"
locator.widget = "textid='67890'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Solid")
locator.area = "ListWidget"
locator.widget = "textid='67249'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)


