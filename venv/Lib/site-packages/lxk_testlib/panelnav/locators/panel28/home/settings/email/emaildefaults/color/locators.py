""" Locators Module
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

# Add screen and components below
screen = Screen()

locator = screen.add_locator("COLOR")
locator.widget = "textid='65938'"

locator = screen.add_locator("Black and White")
locator.widget = "textid='68453'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Gray")
locator.widget = "textid='66019'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Color")
#locator.area = "textid='65964':ascend=2" TODO: remove In area for Action.WAIT_UNTIL_FOUND
locator.widget = "y='131' && type='LockedItemWidget'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Auto")
locator.widget = "textid='65964'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)
