""" home.settings.layout.separatorsheets
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

# Add screen and components below
screen = Screen()

locator = screen.add_locator("SEPARATOR SHEETS", marker=True)
locator.area = "ListWidget"
locator.widget = "textid='67992'"

locator = screen.add_locator("OPTIONS LIST")
locator.widget = "ListWidget"

locator = screen.add_locator("None")
locator.area = "ListWidget"
locator.widget = "textid='67890'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Between Copies")
locator.area = "ListWidget"
locator.widget = "textid='67234'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Between Jobs")
locator.area = "ListWidget"
locator.widget = "textid='67235'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Between Pages")
locator.area = "ListWidget"
locator.widget = "textid='67236'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)