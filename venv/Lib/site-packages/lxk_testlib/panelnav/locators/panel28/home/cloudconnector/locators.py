""" Home.cloudconnector.panel2_8
"""

from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

# Add screen and components below
screen = Screen()

locator = screen.add_locator("Screen Title", marker=True)
locator.widget = "type='Label'"
locator.text = "%s"

locator = screen.add_locator("Profile\Option List")
locator.area = "ListWidget"
locator.widget = "type='Label'"
locator.text = "%s"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s=%s"
)

# Add Profile Name or Email
locator = screen.add_locator("Line Edit")
locator.widget = "type='LineEdit'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

 
# Keyboard Keys for creating email -- Done
locator = screen.add_locator("Keyboard Button")
locator.text="%s"
locator.widget = "type='KeyboardButton'"


# Pop up confirmation button: Cancel, Create, Delete, Yes, No
locator = screen.add_locator("Popup Dialog Button")
locator.widget = "type='QPushButton'"
locator.text = "%s"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s=%s"
)

locator = screen.add_locator("Cloud Connector Info")
locator.widget = "type='ImageButton' && name='titleBarRightButton'"

locator = screen.add_locator("Cloud Connector Add Profile")
locator.widget = "type='ImageButton' && name='titleBarRightButton'"

locator = screen.add_locator("Cloud Connector Delete Profile")
locator.widget = "type='ImageButton' && name='titleBarRightButton2'"

locator = screen.add_locator("Cloud Connector Code")
locator.area = "ListWidget"
locator.widget = "type='QLabel' && width=301 && height=22"

