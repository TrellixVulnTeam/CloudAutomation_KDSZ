""" home.copy
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

screen = Screen()

#--------------------------------------------------------------------#
#COPY SCREEN LOCATORS - OPTIMIZED/MINIMIZED VERSION
#--------------------------------------------------------------------#
locator = screen.add_locator("App Container", marker=True)
locator.widget = "resource-id='android:id/action_bar_container'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

locator = screen.add_locator("Screen Title")
locator.widget = "resource-id='com.lxk.workflow:id/copy_action_bar_title'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Landing Screen Title")
locator.widget = "text='%s' && resource-id='com.lxk.workflow:id/title'"

#--------------------------------------------------------------------#
# Settings that uses this locator: Copy from
#--------------------------------------------------------------------#
locator = screen.add_locator("Copy From Current Value")
locator.area = "com.lxk.workflow:id/copy_from_button"
locator.widget = "resource-id='com.lxk.workflow:id/value' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Copy to
#--------------------------------------------------------------------#
locator = screen.add_locator("Copy To Current Value")
locator.area = "com.lxk.workflow:id/copy_to_button"
locator.widget = "resource-id='com.lxk.workflow:id/value' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Scale
#--------------------------------------------------------------------#
locator = screen.add_locator("Scale Current Value")
locator.area = "com.lxk.workflow:id/scale_button"
locator.widget = "resource-id='com.lxk.workflow:id/value' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Scale Input Value")
locator.area = "com.lxk.workflow:id/scale_control"
locator.widget = "resource-id='com.lxk.workflow:id/settings_input_int' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Scale Helper Text")
locator.area = "com.lxk.workflow:id/setting_content"
locator.widget = "com.lxk.workflow:id/scale_auto_indicator"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Sides
#--------------------------------------------------------------------#
locator = screen.add_locator("Sides Current Value")
locator.area = "com.lxk.workflow:id/sides_button"
locator.widget = "resource-id='com.lxk.workflow:id/value' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Finishing
#--------------------------------------------------------------------#
locator = screen.add_locator("Finishing Current Value")
locator.area = "com.lxk.workflow:id/finishing_button"
locator.widget = "resource-id='com.lxk.workflow:id/value' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Shortcut (*), Overflow (|)
# Send (Send) and Add Button (+)
#--------------------------------------------------------------------#
locator = screen.add_locator("Shortcut Button")
locator.widget = "com.lxk.workflow:id/saveasshortcut"

locator = screen.add_locator("Add Shortcut Button")
locator.widget = "text-id='TXT_CREATE_SHORTCUT' && resource-id='com.lxk.workflow:id/shortcut_name'"

locator = screen.add_locator("Overflow Button")
locator.widget = "com.lxk.workflow:id/overflow"

locator = screen.add_locator("Action Button")
locator.widget = "com.lxk.workflow:id/start"
locator.text = "%s"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)
locator = screen.add_locator("Popup OK Button")
locator.widget = "resource-id='com.lxk.workflow:id/positive_button' && class='android.widget.Button'"

locator = screen.add_locator("Popup Cancel Button")
locator.widget = "resource-id='com.lxk.workflow:id/negative_button' && class='android.widget.Button'"

locator = screen.add_locator("Shortcut Item")
locator.widget= "com.lxk.workflow:id/shortcut_name"
locator.text = "%s"

#--------------------------------------------------------------------#
# Settings that uses this locator: Clear All Values
#--------------------------------------------------------------------#
locator = screen.add_locator("Popup Dialog Title")
locator.widget = "resource-id='com.lxk.workflow:id/label' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Popup Dialog Message")
locator.widget = "resource-id='android:id/message' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Popup Dialog Button")
locator.widget = "text='%s' && class='android.widget.Button'"

locator = screen.add_locator("Dialog List Selector")
locator.area = "ListWidget"
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Popups (i.e. Add shortcut, Content
# Type, Content Source, Original Size, File Type, Recognized Languages
# Background Detection, Color to remove (Color Dropout) )
#--------------------------------------------------------------------#
locator = screen.add_locator("Popup List Title")
locator.widget = "class='android.widget.TextView' && text='%s'"

locator = screen.add_locator("Popup List Item")
locator.widget = "class='android.widget.TextView' && text='%s'"

locator = screen.add_locator("Popup Button Item")
locator.widget = "class='android.widget.Button' && text='%s'"

locator = screen.add_locator("Popup Number Field")
locator.widget = "resource-id='com.lxk.workflow:id/input' && class='android.widget.EditText'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Popup Number OK Button")
locator.widget = "resource-id='com.lxk.workflow:id/ok' && class='android.widget.Button'"

locator = screen.add_locator("Popup Number Cancel Button")
locator.widget = "resource-id='com.lxk.workflow:id/cancel' && class='android.widget.Button'"

#--------------------------------------------------------------------#
# Settings that uses this locator: Clear All Values
#--------------------------------------------------------------------#
locator = screen.add_locator("Overflow List Item")
locator.widget = "class='android.widget.TextView' && text='%s'"

#--------------------------------------------------------------------#
# Settings that uses this locator: Content, etc.
#--------------------------------------------------------------------#
locator = screen.add_locator("Menu List Item")
locator.area = "com.lxk.workflow:id/recyclerlist:swipe_offsety0=15:swipe_offsety1=-15"
locator.widget = "text='%s' && resource-id='com.lxk.workflow:id/title' && class='android.widget.TextView'"

#--------------------------------------------------------------------#
# Settings that uses this locator: Content, etc.
#--------------------------------------------------------------------#
locator = screen.add_locator("Menu List Item Secondary Value")
locator.area = "com.lxk.workflow:id/recyclerlist"
locator.widget = "resource-id='com.lxk.workflow:id/value' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Scan Edge to Edge
#--------------------------------------------------------------------#
locator = screen.add_locator("Text Button")
locator.area = "com.lxk.workflow:id/contentRight"
locator.widget = "text='%s' && class='android.widget.Button'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Numbers
#--------------------------------------------------------------------#
locator = screen.add_locator("TextView Button")
locator.area = "com.lxk.workflow:id/contentRight"
locator.widget = "text='%s' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Custom Overlay Field
#--------------------------------------------------------------------#
locator = screen.add_locator("Custom Overlay Text Field")
locator.area = "com.lxk.workflow:id/contentRight"
locator.widget = "resource-id='com.lxk.workflow:id/overlay_custom' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Separator Sheets, Cover Page Setup
#--------------------------------------------------------------------#
locator = screen.add_locator("Edit Button")
locator.area = "com.lxk.workflow:id/contentRight"
locator.widget = "resource-id='com.lxk.workflow:id/edit_button' && class='android.widget.ImageButton'"

#--------------------------------------------------------------------#
# Settings that uses this locator: Sides in Paper Setup
#--------------------------------------------------------------------#
locator = screen.add_locator("Swipe Up")
locator.area = "com.lxk.workflow:id/scroller"
locator.widget = "text='%s' && class='android.widget.TextView'"

locator = screen.add_locator("Copy Source TextView Button")
locator.area = "com.lxk.workflow:id/copysource"
locator.widget = "text='%s' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Copy Output TextView Button")
locator.area = "com.lxk.workflow:id/copyoutput"
locator.widget = "text='%s' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Overlay
#--------------------------------------------------------------------#
locator = screen.add_locator("Radio Button")
locator.area = "com.lxk.workflow:id/contentRight"
locator.widget = "text='%s' && class='android.widget.RadioButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify checked='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Margin Shift
#--------------------------------------------------------------------#
locator = screen.add_locator("Horizontal Input Value")
locator.area = "com.lxk.workflow:id/margin_shift_x"
locator.widget = "resource-id='com.lxk.workflow:id/settings_input_int' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Horizontal Increment Button")
locator.area = "com.lxk.workflow:id/margin_shift_x"
locator.widget = "resource-id='com.lxk.workflow:id/increment' && class='android.widget.Button'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify enabled='%s'"
)

locator = screen.add_locator("Horizontal Decrement Button")
locator.area = "com.lxk.workflow:id/margin_shift_x"
locator.widget = "resource-id='com.lxk.workflow:id/decrement' && class='android.widget.Button'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify enabled='%s'"
)

locator = screen.add_locator("Vertical Input Value")
locator.area = "com.lxk.workflow:id/margin_shift_y"
locator.widget = "resource-id='com.lxk.workflow:id/settings_input_int' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Vertical Increment Button")
locator.area = "com.lxk.workflow:id/margin_shift_y"
locator.widget = "resource-id='com.lxk.workflow:id/increment' && class='android.widget.Button'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify enabled='%s'"
)

locator = screen.add_locator("Vertical Decrement Button")
locator.area = "com.lxk.workflow:id/margin_shift_y"
locator.widget = "resource-id='com.lxk.workflow:id/decrement' && class='android.widget.Button'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify enabled='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Edge Erase
#--------------------------------------------------------------------#
locator = screen.add_locator("All Edge Erase Button")
locator.area = "com.lxk.workflow:id/contentRight"
locator.widget = "resource-id='com.lxk.workflow:id/edge_erase_button_all' && class='android.widget.ImageButton'"

locator = screen.add_locator("Increment All Edge Erase Button")
locator.widget = "resource-id='com.lxk.workflow:id/increment_all' && class='android.widget.Button'"

locator = screen.add_locator("Decrement All Edge Erase Button")
locator.widget = "resource-id='com.lxk.workflow:id/decrement_all' && class='android.widget.Button'"

locator = screen.add_locator("All Edge Erase Label")
locator.area = "com.lxk.workflow:id/contentRight"
locator.widget = "resource-id='com.lxk.workflow:id/edge_erase_all_label' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Top Edge Erase Button")
locator.widget = "resource-id='com.lxk.workflow:id/edge_erase_button_top' && class='android.widget.Button'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Left Edge Erase Button")
locator.widget = "resource-id='com.lxk.workflow:id/edge_erase_button_left' && class='android.widget.Button'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Bottom Edge Erase Button")
locator.widget = "resource-id='com.lxk.workflow:id/edge_erase_button_bottom' && class='android.widget.Button'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Right Edge Erase Button")
locator.widget = "resource-id='com.lxk.workflow:id/edge_erase_button_right' && class='android.widget.Button'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Edge Erase Input Value")
locator.area = "com.lxk.workflow:id/setting_entry"
locator.widget = "resource-id='com.lxk.workflow:id/settings_input_int' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Edge Erase Increment Button")
locator.area = "com.lxk.workflow:id/setting_entry"
locator.widget = "resource-id='com.lxk.workflow:id/increment' && class='android.widget.Button'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify enabled='%s'"
)

locator = screen.add_locator("Edge Erase Decrement Button")
locator.area = "com.lxk.workflow:id/setting_entry"
locator.widget = "resource-id='com.lxk.workflow:id/decrement' && class='android.widget.Button'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify enabled='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Edge Erase, Number of Copies, Scale
#--------------------------------------------------------------------#
locator = screen.add_locator("Input Value")
locator.widget = "resource-id='com.lxk.workflow:id/settings_input_int' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Increment Button")
locator.widget = "resource-id='com.lxk.workflow:id/increment' && class='android.widget.Button'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify enabled='%s'"
)

locator = screen.add_locator("Decrement Button")
locator.widget = "resource-id='com.lxk.workflow:id/decrement' && class='android.widget.Button'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify enabled='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Advanced Imaging
#--------------------------------------------------------------------#
locator = screen.add_locator("Settings Title")
locator.area = "com.lxk.workflow:id/contentRight"
locator.widget = "text='%s' && class='android.widget.TextView'"

#--------------------------------------------------------------------#
# Settings that uses this locator: Print Page Border
#--------------------------------------------------------------------#
locator = screen.add_locator("Setting List Checkbox")
locator.area = "com.lxk.workflow:id/contentRight"
locator.widget = "class='android.widget.CheckBox'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify checked='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Pages Per Side
#--------------------------------------------------------------------#
locator = screen.add_locator("Toggle Button")
locator.area = "com.lxk.workflow:id/contentRight"
locator.widget = "class='android.widget.ToggleButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Custom Job
#--------------------------------------------------------------------#
locator = screen.add_locator("Custom Job Toggle")
locator.area = "com.lxk.workflow:id/contentRight"
locator.widget = "resource-id='com.lxk.workflow:id/control' && class='android.widget.ToggleButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Custom Job Helper Text")
locator.area = "com.lxk.workflow:id/contentRight"
locator.widget = "resource-id='com.lxk.workflow:id/description' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Left Header
#--------------------------------------------------------------------#
locator = screen.add_locator("Left Header Toggle")
locator.area = "com.lxk.workflow:id/leftHeader"
locator.widget = "class='android.widget.ToggleButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Middle Header
#--------------------------------------------------------------------#
locator = screen.add_locator("Middle Header Toggle")
locator.area = "com.lxk.workflow:id/middleHeader"
locator.widget = "class='android.widget.ToggleButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Right Header
#--------------------------------------------------------------------#
locator = screen.add_locator("Right Header Toggle")
locator.area = "com.lxk.workflow:id/rightHeader"
locator.widget = "class='android.widget.ToggleButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Left Footer
#--------------------------------------------------------------------#
locator = screen.add_locator("Left Footer Toggle")
locator.area = "com.lxk.workflow:id/leftFooter"
locator.widget = "class='android.widget.ToggleButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Middle Footer
#--------------------------------------------------------------------#
locator = screen.add_locator("Middle Footer Toggle")
locator.area = "com.lxk.workflow:id/middleFooter"
locator.widget = "class='android.widget.ToggleButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Right Footer
#--------------------------------------------------------------------#
locator = screen.add_locator("Right Footer Toggle")
locator.area = "com.lxk.workflow:id/rightFooter"
locator.widget = "class='android.widget.ToggleButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Mirror Image
#--------------------------------------------------------------------#
locator = screen.add_locator("Mirror Image Toggle")
locator.area = "com.lxk.workflow:id/contentRight"
locator.widget = "resource-id='com.lxk.workflow:id/mirror_control' && class='android.widget.ToggleButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Auto Center
#--------------------------------------------------------------------#
locator = screen.add_locator("Auto Center Toggle")
locator.area = "com.lxk.workflow:id/setting_entry"
locator.widget = "resource-id='com.lxk.workflow:id/auto_center_control' && class='android.widget.ToggleButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Negative Image
#--------------------------------------------------------------------#
locator = screen.add_locator("Negative Image Toggle")
locator.area = "com.lxk.workflow:id/contentRight"
locator.widget = "resource-id='com.lxk.workflow:id/negative_control' && class='android.widget.ToggleButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: ADF Skew
#--------------------------------------------------------------------#
locator = screen.add_locator("ADF Skew Toggle")
locator.area = "com.lxk.workflow:id/contentRight"
locator.widget = "resource-id='com.lxk.workflow:id/deskew_control' && class='android.widget.ToggleButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Darkness
#--------------------------------------------------------------------#
locator = screen.add_locator("Darkness Slider Left Label")
locator.area = "com.lxk.workflow:id/darkness_seekbar"
locator.widget = "resource-id='com.lxk.workflow:id/seekbar_left_label' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Darkness Slider Right Label")
locator.area = "com.lxk.workflow:id/darkness_seekbar"
locator.widget = "resource-id='com.lxk.workflow:id/seekbar_right_label' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Darkness Slider Minimum Value")
locator.area = "com.lxk.workflow:id/darkness_seekbar"
locator.widget = "resource-id='com.lxk.workflow:id/min' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Darkness Slider Maximum Value")
locator.area = "com.lxk.workflow:id/darkness_seekbar"
locator.widget = "resource-id='com.lxk.workflow:id/max' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Darkness Current Value")
locator.area = "com.lxk.workflow:id/darkness_seekbar"
locator.widget = "resource-id='com.lxk.workflow:id/value' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Darkness Slider Value")
locator.area = "com.lxk.workflow:id/darkness_seekbar"
locator.widget = "android.widget.SeekBar"
locator.add_action(
    action=Action.SELECT,
    specifier="set seekbar='%s':widget='com.lxk.workflow:id/value'"
)

locator = screen.add_locator("Darkness Landing Screen Current Value")
locator.area = "com.lxk.workflow:id/darkness_button"
locator.widget = "resource-id='com.lxk.workflow:id/value' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Shadow Detail
#--------------------------------------------------------------------#
locator = screen.add_locator("Shadow Detail Slider Left Label")
locator.area = "com.lxk.workflow:id/shadow_detail_control"
locator.widget = "resource-id='com.lxk.workflow:id/seekbar_left_label' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Shadow Detail Slider Right Label")
locator.area = "com.lxk.workflow:id/shadow_detail_control"
locator.widget = "resource-id='com.lxk.workflow:id/seekbar_right_label' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Shadow Detail Slider Minimum Value")
locator.area = "com.lxk.workflow:id/shadow_detail_control"
locator.widget = "resource-id='com.lxk.workflow:id/min' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Shadow Detail Slider Maximum Value")
locator.area = "com.lxk.workflow:id/shadow_detail_control"
locator.widget = "resource-id='com.lxk.workflow:id/max' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Shadow Detail Current Value")
locator.area = "com.lxk.workflow:id/shadow_detail_control"
locator.widget = "resource-id='com.lxk.workflow:id/value' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Shadow Detail Slider Value")
locator.area = "com.lxk.workflow:id/shadow_detail_control"
locator.widget = "android.widget.SeekBar"
locator.add_action(
    action=Action.SELECT,
    specifier="set seekbar='%s':widget='com.lxk.workflow:id/value'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Background Removal
#--------------------------------------------------------------------#
locator = screen.add_locator("Background Removal Slider Left Label")
locator.area = "com.lxk.workflow:id/background_removal_control"
locator.widget = "resource-id='com.lxk.workflow:id/seekbar_left_label' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Background Removal Slider Right Label")
locator.area = "com.lxk.workflow:id/background_removal_control"
locator.widget = "resource-id='com.lxk.workflow:id/seekbar_right_label' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Background Removal Slider Minimum Value")
locator.area = "com.lxk.workflow:id/bacndroid.widget.ListViewkground_removal_control"
locator.widget = "resource-id='com.lxk.workflow:id/min' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Background Removal Slider Maximum Value")
locator.area = "com.lxk.workflow:id/background_removal_control"
locator.widget = "resource-id='com.lxk.workflow:id/max' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Background Removal Current Value")
locator.area = "com.lxk.workflow:id/background_removal_control"
locator.widget = "resource-id='com.lxk.workflow:id/value' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Background Removal Slider Value")
locator.area = "com.lxk.workflow:id/background_removal_control"
locator.widget = "android.widget.SeekBar"
locator.add_action(
    action=Action.SELECT,
    specifier="set seekbar='%s':widget='com.lxk.workflow:id/value'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Sharpness
#--------------------------------------------------------------------#
locator = screen.add_locator("Sharpness Slider Left Label")
locator.area = "com.lxk.workflow:id/sharpness_control"
locator.widget = "resource-id='com.lxk.workflow:id/seekbar_left_label' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Sharpness Slider Right Label")
locator.area = "com.lxk.workflow:id/sharpness_control"
locator.widget = "resource-id='com.lxk.workflow:id/seekbar_right_label' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Sharpness Slider Minimum Value")
locator.area = "com.lxk.workflow:id/sharpness_control"
locator.widget = "resource-id='com.lxk.workflow:id/min' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Sharpness Slider Maximum Value")
locator.area = "com.lxk.workflow:id/sharpness_control"
locator.widget = "resource-id='com.lxk.workflow:id/max' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Sharpness Current Value")
locator.area = "com.lxk.workflow:id/sharpness_control"
locator.widget = "resource-id='com.lxk.workflow:id/value' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Sharpness Slider Value")
locator.area = "com.lxk.workflow:id/sharpness_control"
locator.widget = "android.widget.SeekBar"
locator.add_action(
    action=Action.SELECT,
    specifier="set seekbar='%s':widget='com.lxk.workflow:id/value'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Contrast
#--------------------------------------------------------------------#
locator = screen.add_locator("Contrast Best For Content")
locator.area = "com.lxk.workflow:id/scroller"
locator.widget = "resource-id='com.lxk.workflow:id/lock_check_box' && class='android.widget.CheckedTextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify checked='%s'"
)

locator = screen.add_locator("Contrast Slider Left Label")
locator.area = "com.lxk.workflow:id/contrast_control"
locator.widget = "resource-id='com.lxk.workflow:id/seekbar_left_label' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Contrast Slider Right Label")
locator.area = "com.lxk.workflow:id/contrast_control"
locator.widget = "resource-id='com.lxk.workflow:id/seekbar_right_label' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Contrast Swipe Up")
locator.area = "com.lxk.workflow:id/scroller"
locator.widget = "text='0' && resource-id='com.lxk.workflow:id/min'"
locator.add_action(
    action=Action.PRESS,
    specifier="nothing"
)

locator = screen.add_locator("Contrast Slider Minimum Value")
locator.area = "com.lxk.workflow:id/contrast_control"
locator.widget = "resource-id='com.lxk.workflow:id/min' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Contrast Slider Maximum Value")
locator.area = "com.lxk.workflow:id/contrast_control"
locator.widget = "resource-id='com.lxk.workflow:id/max' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Contrast Current Value")
locator.area = "com.lxk.workflow:id/contrast_control"
locator.widget = "resource-id='com.lxk.workflow:id/value' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Contrast Slider Value")
locator.area = "com.lxk.workflow:id/seek_bar_container"
locator.widget = "android.widget.SeekBar"
locator.add_action(
    action=Action.SELECT,
    specifier="set seekbar='%s':widget='com.lxk.workflow:id/value'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Temperature
#--------------------------------------------------------------------#
locator = screen.add_locator("Temperature Slider Left Label")
locator.area = "com.lxk.workflow:id/temperature_control"
locator.widget = "resource-id='com.lxk.workflow:id/seekbar_left_label' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Temperature Slider Right Label")
locator.area = "com.lxk.workflow:id/temperature_control"
locator.widget = "resource-id='com.lxk.workflow:id/seekbar_right_label' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Temperature Slider Minimum Value")
locator.area = "com.lxk.workflow:id/temperature_control"
locator.widget = "resource-id='com.lxk.workflow:id/min' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Temperature Slider Maximum Value")
locator.area = "com.lxk.workflow:id/temperature_control"
locator.widget = "resource-id='com.lxk.workflow:id/max' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Temperature Current Value")
locator.area = "com.lxk.workflow:id/temperature_control"
locator.widget = "resource-id='com.lxk.workflow:id/value' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Temperature Slider Value")
locator.area = "com.lxk.workflow:id/temperature_control:swipe_offsety1=-10"
locator.widget = "android.widget.SeekBar"
locator.add_action(
    action=Action.SELECT,
    specifier="set seekbar='%s':widget='com.lxk.workflow:id/value'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Color Dropout
#--------------------------------------------------------------------#
locator = screen.add_locator("Color To Remove")
locator.widget = "text-id='TXT_COLOR_TO_REMOVE'"

locator = screen.add_locator("Color Dropout Slider Left Label")
locator.area = "com.lxk.workflow:id/color_dropout"
locator.widget = "resource-id='com.lxk.workflow:id/seekbar_left_label' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Color Dropout Slider Right Label")
locator.area = "com.lxk.workflow:id/color_dropout"
locator.widget = "resource-id='com.lxk.workflow:id/seekbar_right_label' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Color Dropout Slider Minimum Value")
locator.area = "com.lxk.workflow:id/color_dropout"
locator.widget = "resource-id='com.lxk.workflow:id/min' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Color Dropout Slider Maximum Value")
locator.area = "com.lxk.workflow:id/color_dropout"
locator.widget = "resource-id='com.lxk.workflow:id/max' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#-------------------------------------------------------#
# Settings that uses this locator: Red Color Dropout
#-------------------------------------------------------#
locator = screen.add_locator("Red Dropout Current Value")
locator.area = "com.lxk.workflow:id/dropout_red_control"
locator.widget = "resource-id='com.lxk.workflow:id/value' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Red Dropout Slider Value")
locator.area = "com.lxk.workflow:id/dropout_red_control"
locator.widget = "android.widget.SeekBar"
locator.add_action(
    action=Action.SELECT,
    specifier="set seekbar='%s':widget='com.lxk.workflow:id/value'"
)

#-------------------------------------------------------#
# Settings that uses this locator: Green Color Dropout
#-------------------------------------------------------#
locator = screen.add_locator("Green Dropout Current Value")
locator.area = "com.lxk.workflow:id/dropout_green_control"
locator.widget = "resource-id='com.lxk.workflow:id/value' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Green Dropout Slider Value")
locator.area = "com.lxk.workflow:id/dropout_green_control"
locator.widget = "android.widget.SeekBar"
locator.add_action(
    action=Action.SELECT,
    specifier="set seekbar='%s':widget='com.lxk.workflow:id/value'"
)

#-------------------------------------------------------#
# Settings that uses this locator: Blue Color Dropout
#-------------------------------------------------------#
locator = screen.add_locator("Blue Dropout Current Value")
locator.area = "com.lxk.workflow:id/dropout_blue_control"
locator.widget = "resource-id='com.lxk.workflow:id/value' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Blue Dropout Slider Value")
locator.area = "com.lxk.workflow:id/dropout_blue_control"
locator.widget = "android.widget.SeekBar"
locator.add_action(
    action=Action.SELECT,
    specifier="set seekbar='%s':widget='com.lxk.workflow:id/value'"
)

#--------------------------------------------------------------------#
# Settings that uses this locator: Color Balance
#--------------------------------------------------------------------#
locator = screen.add_locator("Cyan-Red Slider Left Label")
locator.area = "com.lxk.workflow:id/red_balance_control"
locator.widget = "resource-id='com.lxk.workflow:id/seekbar_left_label' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Cyan-Red Slider Right Label")
locator.area = "com.lxk.workflow:id/red_balance_control"
locator.widget = "resource-id='com.lxk.workflow:id/seekbar_right_label' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Cyan-Red Slider Minimum Value")
locator.area = "com.lxk.workflow:id/red_balance_control"
locator.widget = "resource-id='com.lxk.workflow:id/min' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Cyan-Red Slider Maximum Value")
locator.area = "com.lxk.workflow:id/red_balance_control"
locator.widget = "resource-id='com.lxk.workflow:id/max' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Cyan-Red Current Value")
locator.area = "com.lxk.workflow:id/red_balance_control"
locator.widget = "resource-id='com.lxk.workflow:id/value' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Cyan-Red Slider Value")
locator.area = "com.lxk.workflow:id/red_balance_control"
locator.widget = "android.widget.SeekBar"
locator.add_action(
    action=Action.SELECT,
    specifier="set seekbar='%s':widget='com.lxk.workflow:id/value'"
)

locator = screen.add_locator("Magenta-Green Slider Left Label")
locator.area = "com.lxk.workflow:id/green_balance_control"
locator.widget = "resource-id='com.lxk.workflow:id/seekbar_left_label' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Magenta-Green Slider Right Label")
locator.area = "com.lxk.workflow:id/green_balance_control"
locator.widget = "resource-id='com.lxk.workflow:id/seekbar_right_label' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Magenta-Green Slider Minimum Value")
locator.area = "com.lxk.workflow:id/green_balance_control"
locator.widget = "resource-id='com.lxk.workflow:id/min' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Magenta-Green Slider Maximum Value")
locator.area = "com.lxk.workflow:id/green_balance_control"
locator.widget = "resource-id='com.lxk.workflow:id/max' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Magenta-Green Current Value")
locator.area = "com.lxk.workflow:id/green_balance_control"
locator.widget = "resource-id='com.lxk.workflow:id/value' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Magenta-Green Slider Value")
locator.area = "com.lxk.workflow:id/green_balance_control"
locator.widget = "android.widget.SeekBar"
locator.add_action(
    action=Action.SELECT,
    specifier="set seekbar='%s':widget='com.lxk.workflow:id/value'"
)

locator = screen.add_locator("Yellow-Blue Slider Left Label")
locator.area = "com.lxk.workflow:id/blue_balance_control"
locator.widget = "resource-id='com.lxk.workflow:id/seekbar_left_label' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Yellow-Blue Slider Right Label")
locator.area = "com.lxk.workflow:id/blue_balance_control"
locator.widget = "resource-id='com.lxk.workflow:id/seekbar_right_label' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Yellow-Blue Slider Minimum Value")
locator.area = "com.lxk.workflow:id/blue_balance_control"
locator.widget = "resource-id='com.lxk.workflow:id/min' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Yellow-Blue Slider Maximum Value")
locator.area = "com.lxk.workflow:id/blue_balance_control"
locator.widget = "resource-id='com.lxk.workflow:id/max' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Yellow-Blue Current Value")
locator.area = "com.lxk.workflow:id/blue_balance_control"
locator.widget = "resource-id='com.lxk.workflow:id/value' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Yellow-Blue Slider Value")
locator.area = "com.lxk.workflow:id/blue_balance_control"
locator.widget = "android.widget.SeekBar"
locator.add_action(
    action=Action.SELECT,
    specifier="set seekbar='%s':widget='com.lxk.workflow:id/value'"
)
