""" home
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen
# from . import (
    #
# )

screen = Screen()

locator = screen.add_locator("Separator Sheets Content Right", marker=True)
locator.area = "com.lxk.workflow:id/contentRight"
locator.widget = "resource-id='com.lxk.workflow:id/setting_entry'"

locator = screen.add_locator("Separator Sheets Toggle")
locator.area = "ScrollView"
locator.widget = "resource-id='com.lxk.workflow:id/separator_switch' && class='android.widget.ToggleButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)
