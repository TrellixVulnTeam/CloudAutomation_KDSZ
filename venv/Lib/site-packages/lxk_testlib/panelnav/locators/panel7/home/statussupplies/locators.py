""" panel7.home.statussupplies
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

screen = Screen()

#--------------------------------------------------------------------#
#STATUS/SUPPLIES SCREEN LOCATORS - OPTIMIZED/MINIMIZED VERSION
#--------------------------------------------------------------------#

locator = screen.add_locator("Screen Title", marker=True)
locator.widget = "resource-id='android:id/action_bar_title'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Menu that uses this locator:
# main screen= Trays, Alerts, Supplies
#--------------------------------------------------------------------#

locator = screen.add_locator("Menu List")
locator.widget = "resource-id='com.lxk.Status:id/NavItemListView' && class='android.widget.ListView'"

locator = screen.add_locator("Menu List Item")
locator.area = "resource-id='com.lxk.Status:id/NavItemListView' && class='android.widget.ListView'"
locator.widget = "text='%s' && resource-id='com.lxk.Status:id/title'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Menu that uses this locator:
# alerts= Warnings*
# trays= Tray 1, Manual Paper, Manual Envelope,
# supplies= Black Cartridge, Cyan Cartridge, Magenta Cartridge,
# Yellow Cartridge, Waste Toner Bottle
#--------------------------------------------------------------------#

locator = screen.add_locator("Status List Item")
locator.area = "resource-id='com.lxk.Status:id/ItemListView' && class='android.widget.ListView'"
locator.widget = "text='%s' && resource-id='com.lxk.Status:id/title'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Status List Page Remaining")
locator.area = "resource-id='com.lxk.Status:id/ItemListView' && class='android.widget.ListView'"
locator.text = '%s'
locator.widget = "resource-id='com.lxk.Status:id/pages_remaining'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

locator = screen.add_locator("Status Page Remaining")
locator.area = "resource-id='com.lxk.Status:id/ItemListView' && class='android.widget.ListView'"
locator.widget = "text='%s' && resource-id='com.lxk.Status:id/pages_remaining'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

#--------------------------------------------------------------------#
# Menu that uses this locator:
# trays= Tray 1, Manual Paper, Manual Envelope SIZE and TYPE Values
#--------------------------------------------------------------------#

locator = screen.add_locator("Tray Value")
locator.area = "resource-id='com.lxk.Status:id/Tray 1' or resource-id='com.lxk.Status:id/Tray 2'"
locator.widget = "text='%s' && resource-id='com.lxk.Status:id/title'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Multipurpose Feeder Value")
locator.area = "resource-id='com.lxk.Status:id/Multipurpose Feeder'"
locator.widget = "text='%s' && resource-id='com.lxk.Status:id/title'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Paper Value")
locator.area = "resource-id='com.lxk.Status:id/Manual Paper'"
locator.widget = "text='%s' && resource-id='com.lxk.Status:id/title'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Envelope Value")
locator.area = "resource-id='com.lxk.Status:id/Manual Envelope'"
locator.widget = "text='%s' && resource-id='com.lxk.Status:id/title'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#--------------------------------------------------------------------#
# Menu that uses this locator: All Popup Dialog
#--------------------------------------------------------------------#

locator = screen.add_locator("Popup List")
locator.widget = "resource-id='android:id/select_dialog_listview' && class='android.widget.ListView'"

locator = screen.add_locator("Popup List Title")
locator.widget = "resource-id='android:id/alertTitle' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Popup List Item")
locator.area = "resource-id='android:id/select_dialog_listview' && class='android.widget.ListView'"
locator.widget = "text='%s' && resource-id='com.lxk.Status:id/title'"

#-----------------------------------------------------------------------------#
##### IR Screen Locators #####
#-----------------------------------------------------------------------------#
locator = screen.add_locator("IR Screen Title")
locator.widget = "text='%s' && resource-id='com.lxk.Status:id/title' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

locator = screen.add_locator("IR Screen Button")
locator.area = "resource-id='com.lxk.Status:id/topBar'"
locator.widget = "text='%s' && class='android.widget.Button'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

locator = screen.add_locator("IR Video Frame")
locator.widget = "resource-id='com.lxk.Status:id/steps_view'"

locator = screen.add_locator("IR Screen Video")
locator.area = "resource-id='com.lxk.Status:id/image_fragment'"
locator.widget = "resource-id='com.lxk.Status:id/video' && class='android.widget.VideoView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

locator = screen.add_locator("IR Screen Video ProgressBar")
locator.area = "resource-id='com.lxk.Status:id/image_fragment'"
locator.widget = "resource-id='com.lxk.Status:id/progress' && class='android.widget.ProgressBar'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

locator = screen.add_locator("IR Screen Step Text")
locator.area = "resource-id='com.lxk.Status:id/steps_fragment'"
locator.widget = "resource-id='com.lxk.Status:id/steptext' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

locator = screen.add_locator("IR Screen Step Frame")
locator.area = "resource-id='com.lxk.Status:id/steps_view'"
locator.widget = "text='%s' && resource-id='com.lxk.Status:id/description' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

locator = screen.add_locator("IR Screen Step Counter")
locator.area = "resource-id='com.lxk.Status:id/steps_view'"
locator.widget = "resource-id='com.lxk.Status:id/step_counter' && class='android.widget.TextView'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

locator = screen.add_locator("IR Screen List Item")
locator.area = "resource-id='android:id/list' && class='android.widget.ListView'"
locator.widget = "text='%s' && resource-id='com.lxk.Status:id/title'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

#-----------------------------------------------------------------------------#
##### IR Screen Locators End#####
#-----------------------------------------------------------------------------#
