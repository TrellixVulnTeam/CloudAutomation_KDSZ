""" home.copy.copyfrom
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

screen = Screen()

locator = screen.add_locator("Letter (8.5 x 11 in.)")
locator.widget = "textid='67159'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Legal (8.5 x 14 in.)")
locator.widget = "textid='67160'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)