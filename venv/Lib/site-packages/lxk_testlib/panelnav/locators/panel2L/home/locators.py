""" home
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen
from . import (
    settings,
    networkstatus,
    copy,
    idcardcopy,
    fax,
    shortcuts,
    semenu,
    diagnostics,
    heldjobs
)

screen = Screen()

locator = screen.add_locator("Status", marker=True)
locator.widget = "textid='67964'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Network Status")
locator.widget = "textid='71407'"
locator.next_screen = networkstatus

locator = screen.add_locator("Copy")
locator.area = "list"
locator.widget = "textid='65977'"
locator.next_screen = copy

locator = screen.add_locator("ID Card Copy")
locator.widget = "textid='71917'"
locator.next_screen = idcardcopy

locator = screen.add_locator("Fax")
locator.widget = "textid='66046'"
locator.next_screen = fax

locator = screen.add_locator("Shortcuts")
locator.widget = "textid='67977'"
locator.next_screen = shortcuts

locator = screen.add_locator("Settings")
locator.widget = "textid='67984'"
locator.next_screen = settings

locator = screen.add_locator("Status Label")
locator.widget = "type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Held Jobs")
locator.widget = "textid='66990'"
locator.next_screen = heldjobs

###################### INITIAL SETUP WIZARD SETTINGS LIST ######################
##Verify Panel Locator Exist       Setup Screen Title       text=<screen_title>
locator = screen.add_locator("Setup Screen Title")
locator.widget = "type='Label'"
locator.text = "%s"

#Verify Panel Locator Exist       Setup Helper Text       text=<helper_text>
#Verify Panel Locator Exist       Setup Helper Text       text=lexmark.com/deviceinformation
locator = screen.add_locator("Setup Helper Text")
locator.widget = "text='%s' && type='Label'"

#Verify Panel Locator Exist       Setup Menu List Item         <value>
#Press Panel Locator              Setup Menu List Item         text=<setting>
locator = screen.add_locator("Setup Menu List Item")
locator.widget="text='%s' && type='DataObjectListItem' or type='StringListItem'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

#Verify Panel Locator Attribute   Setup List Option         <selected>      text=<value>
#Press Panel Locator              Setup List Option         text=<setting>
locator = screen.add_locator("Setup List Option")
locator.widget = "text='%s' && type='IntegerEnumListItem'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

#Verify Panel Locator Attribute   Setup EditBox     <value>
locator = screen.add_locator("Setup EditBox")
locator.widget = "type='TextEntry'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

### Manual Date and Time ###
#Verify Panel Locator Attribute     Manual Set Date     07-23-2023
locator = screen.add_locator("Manual Set Date")
locator.widget = "type='DateChanger'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='[%s]'"
)
#Verify Panel Locator Attribute     Manual Set Time     05:45 AM
locator = screen.add_locator("Manual Set Time")
locator.widget = "type='TimeChanger'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='[%s]'"
)

#-----------------------------------------------------------------------------#
# Notification Area Locators / Display Info
#-----------------------------------------------------------------------------#
locator = screen.add_locator("Screen Display Info")
locator.area = "objects:index=2"
locator.widget = "type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Notification Display Info Value")
locator.area = "objects:index=2"
locator.widget = "text='%s' && type='Label'"

#-----------------------------------------------------------------------------#
# Access SE and Diagnostic Menu
#-----------------------------------------------------------------------------#

locator = screen.add_locator("SE Menu")
locator.next_screen = semenu
locator.add_action(
    action=Action.PRESS_KEY_COMBINATION,
    specifier="KEY_Backspace, KEY_Left, KEY_Backspace, KEY_Left"
)

locator = screen.add_locator("Diagnostics")
locator.next_screen = diagnostics
locator.add_action(
    action=Action.PRESS_KEY_COMBINATION,
    specifier="KEY_Backspace, KEY_Right, KEY_Backspace, KEY_Right"
)

# IR Screen Locators #
#-----------------------------------------------------------------------------#
locator = screen.add_locator("IR Screen Title")
locator.area = "objects:index=1"
locator.widget = "type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

locator = screen.add_locator("IR Screen Title Without Index")
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

locator = screen.add_locator("IR Screen Button")
locator.area = "lists"
locator.widget = "text= '%s' && type='DataObjectListItem '"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

