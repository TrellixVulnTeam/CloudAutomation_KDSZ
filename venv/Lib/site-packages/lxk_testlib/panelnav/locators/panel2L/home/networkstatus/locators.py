""" Locators Module
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen

screen = Screen()

locator = screen.add_locator("Ethernet", marker=True)
locator.widget = "textid='69221'"

locator = screen.add_locator("Network Speed")
locator.widget = "textid='72966'"

locator = screen.add_locator("Network Address")
locator.widget = "textid='69146'"

locator = screen.add_locator("Energy Efficient Ethernet")
locator.widget = "textid='72668'"

locator = screen.add_locator("ETHERNET_TEXT")
locator.widget = "textid='69221'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("NETWORK_SPEED_TEXT")
locator.widget = "textid='72966'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("NETWORK_ADDRESS_TEXT")
locator.widget = "textid='69146'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("ENERGY_EFFICIENT_ETHERNET_TEXT")
locator.widget = "textid='72668'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)
