""" home.settings
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen
from . import (
    device,
    fax,
    copy
)

screen = Screen()

locator = screen.add_locator("Device Menu")
locator.widget = "textid='72728'"
locator.next_screen = device

locator = screen.add_locator("Fax Menu")
locator.widget = "textid='66046'"
locator.next_screen = fax

locator = screen.add_locator("Copy Menu")
locator.widget = "textid='65977'"
locator.next_screen = copy

#-----------------------------------------------------------------------------#
#SETTINGS SCREEN LOCATORS - OPTIMIZED/MINIMIZED VERSION
#-----------------------------------------------------------------------------#
locator = screen.add_locator("Screen Title", marker=True)
locator.widget = "type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Menu List Item")
locator.widget = "text='%s' && (type='StateNodeListItem' or type='StringListItem')"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Popup List")
locator.widget = "name='option_0'"

locator = screen.add_locator("Popup List Title")
locator.widget = "text='%s' && type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Popup List Value")
locator.widget = "text='%s' && (type='StringEnumListItem' or 'IntegerEnumListItem')"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Option textvalue")
locator.area = "objects:index=2"
locator.widget = "type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Dialog List Selector")
locator.text = "%s"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

locator = screen.add_locator("Number Selector")
locator.widget = "type='IntegerModel'"
locator.add_action(
    action=Action.SELECT,
    specifier="h-select:data_type=int:set value='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify %s='%s'"
)

locator = screen.add_locator("Keyboard Edit")
locator.widget = "type='TextEntry'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Date")
locator.widget = "type='DateChanger'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

locator = screen.add_locator("Manual Time")
locator.widget = "type='TimeChanger'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)
#-----------------------------------------------------------------------------#
#SETTINGS SCREEN LOCATORS - OPTIMIZED/MINIMIZED VERSION - END OF LOCATORS
#-----------------------------------------------------------------------------#
