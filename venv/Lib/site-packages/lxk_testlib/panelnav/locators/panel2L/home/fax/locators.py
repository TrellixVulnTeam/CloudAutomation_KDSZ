""" home.fax
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen


screen = Screen()

#----------------------------------------------------#
#Settings: Fax Title
#----------------------------------------------------#
locator = screen.add_locator("Screen Title", marker=True)
locator.widget = "type='Label'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)

#----------------------------------------------------#
#Settings: Fax To, Redial, Go Off Hook, Color,
#Original Size, Content, Darkness
#----------------------------------------------------#

locator = screen.add_locator("Menu List Item")
locator.widget = "text='%s' && type='DataObjectListItem'"

#----------------------------------------------------#
#Settings: Color, Original Size, Content, Resolution,
#Scan Edge to Edge, Transmission Log
#----------------------------------------------------#
locator = screen.add_locator("Popup List Value")
locator.widget = "text='%s' && type='IntegerEnumListItem'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify selected='%s'"
)

#----------------------------------------------------#
#Settings: Contrast, Background Removal
#----------------------------------------------------#
locator = screen.add_locator("Number Selector")
locator.widget = "type='IntegerModel'"
locator.add_action(
    action=Action.SELECT,
    specifier="h-select:data_type=int:set value='%s'"
)
locator.add_action(
    action=Action.VERIFY,
    specifier="verify value='%s'"
)

#----------------------------------------------------#
#Setting: Specific to Darkness
#----------------------------------------------------#
locator = screen.add_locator("Darkness Item")
locator.widget = "type='DarknessModel'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify value='%s'"
)
