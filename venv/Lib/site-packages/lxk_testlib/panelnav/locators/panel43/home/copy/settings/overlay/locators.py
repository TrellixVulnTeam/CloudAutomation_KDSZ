""" home.copy.settings.overlay
"""
from lxk_testlib.panelnav.core.enum import Action, Filter
from lxk_testlib.panelnav.core.models.screen import Screen
# from . import (
    #
# )

screen = Screen()

locator = screen.add_locator("Presets", marker=True)
locator.widget = "text-id='TXT_PRESETS_COLON'"

locator = screen.add_locator("Overlay Switch")
locator.area = "ScrollView"
locator.widget = "resource-id='com.lxk.workflow:id/toggle_fullrow_text_right_switch' && class='android.widget.ToggleButton'"
locator.add_action(
    action=Action.VERIFY,
    specifier="verify text='%s'"
)
