"""
lxk_printer_device
"""

from lxk_printer_device._utils import logger

# Automatically on import, INFO and higher level
# logs are configured to stream to console.
# NOTE: to add a file handler (log to file) for DEBUG,
#       call logger.add_file_handler(filename, path)
#       on runtime.
logger.create_logger()
logger.add_stream_handler()
