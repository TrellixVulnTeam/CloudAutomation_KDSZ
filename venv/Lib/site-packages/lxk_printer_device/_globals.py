"""
globals
"""

import os
import platform

PACKAGE_NAME = "lxk_printer_device"
ROOT_DIR = os.path.dirname(os.path.realpath(__file__))
AUTOMATION_LICENSE = os.path.join(
    ROOT_DIR,
    "automation_license",
    "Printer.OpPanel.Automation.zip"
)
FOLDER_PATH = os.path.join(ROOT_DIR, "_files")
LOG_FORMAT = "%(asctime)s - %(filename)s:%(funcName)s:%(lineno)d - %(levelname)s - %(message)s"

# system assert (private)
def assert_linux():
    """
    TODO
    """
    system = platform.system()
    if system != "Linux":
        raise RuntimeError("%s not supported!" % system)
